# CPS 智能巡检系统产品需求文档

> 版本：1.0  
> 日期：2026 年 9 月 10 日  
> 文档状态：产品设计稿

## 1. 文档概述

本 PRD 定义 CPS 巡检场景下的业务闭环、主 Agent 动态调度、人工确认机制、旁路观察 Agent 与长期记忆能力。

系统的核心原则是：

> AI 负责提出建议，人工确认或修正下一步动作，系统记录决策并持续优化。

| 项目 | 内容 |
|---|---|
| 产品名称 | CPS 智能巡检系统 |
| 目标用户 | PDA 巡检员工、拉线主管、整改负责人、巡检管理员 |
| 核心场景 | 产线问题发现、整改跟踪、整改复查、报告汇总与持续优化 |
| 技术方向 | LangChain + LangGraph Runtime + DeepAgents + 多模态模型 + 向量库 |
| 本版本目标 | 打通一次巡检从采集到确认、入库和任务生成的可审计闭环 |

## 2. 背景与目标

### 2.1 业务背景

员工通过 PDA 检查产线问题，现场上传图片并跟踪整改。系统需要统一现场信息、历史巡检记录和整改证据，减少人工汇总成本，提升问题识别、整改判断和后续工作安排的质量。

### 2.2 产品目标

1. 将现场图片、拉线改造信息和历史巡检记录沉淀为统一巡检任务。
2. 通过多个专用 Agent 协作完成问题识别、整改判断、历史分析、报告生成和覆盖分析。
3. 每次调用下一步 Agent 前引入人工确认，允许批准、修改、跳过或退回。
4. 将人工调度选择、修改原因和后续结果沉淀为长期记忆，经审核后用于优化未来流程与决策。
5. 所有最终业务事实、报告入库和任务发布均可追溯、可审计。

### 2.3 非目标

1. 本期不自动替代巡检人员对业务结论的最终确认。
2. 本期不允许观察 Agent 直接修改主 Agent 的 Prompt、规则或线上策略。
3. 统计指标由结构化查询和代码计算，模型负责解释和提出建议，不负责伪造统计结果。

## 3. 用户与权限

| 角色 | 主要职责 | 关键权限 |
|---|---|---|
| 巡检员工 | 创建任务、采集图片、补充现场信息 | 提交任务、修改本人未确认内容 |
| 拉线主管 | 确认问题与整改状态、补充整改要求 | 确认、编辑、退回问题和整改结论 |
| 巡检管理员 | 确认报告、入库、审核记忆和优化建议 | 发布报告、审核长期记忆、管理规则 |
| 系统管理员 | 配置模型、Agent、工具和权限 | 配置与审计，不参与业务结论确认 |

## 4. 总体业务流程

系统以 `InspectionCase`（巡检任务）为核心对象。主 Agent 不使用预先固定的业务节点顺序，而是根据当前状态和人工反馈持续提出下一步动作。

```text
创建巡检任务
    ↓
PDA 采集现场信息和问题图片
    ↓
主 Agent 分析当前状态
    ↓
主 Agent 提议下一步 Agent
    ↓
人工确认 / 修改下一步 Agent
    ↓
执行被选中的 Agent
    ↓
结果返回主 Agent
    ↓
主 Agent 再次判断下一步
    ↓
问题确认 → 整改复查 → 报告确认 → 结构化入库
    ↓
生成下一步工作安排
    ↓
观察 Agent 提炼经验并形成候选长期记忆
    ↓
管理员审核记忆并反哺后续决策
```

| 阶段 | 流程 | 人工参与点 | 输出 |
|---|---|---|---|
| 现场采集 | 创建任务 → PDA 上传问题图片 → 补充位置和备注 | 补充或修正现场信息 | 现场问题证据 |
| 问题分析 | 主 Agent 调用问题识别 Agent → 生成候选结论 | 确认、修改、补充或驳回问题 | 已确认问题 |
| 整改复查 | 提交整改后图片 → 主 Agent 调用整改判断 Agent | 确认是否执行判断、确认整改结论 | 最终整改状态 |
| 报告闭环 | 调用历史分析、覆盖分析、报告 Agent | 确认报告、确认入库 | HTML 报告与结构化数据 |
| 持续优化 | 观察 Agent 监听链路 → 筛选记忆 → 审核后入库 | 审核记忆与优化建议 | 长期记忆与优化策略 |

## 5. 主 Agent 动态调度

### 5.1 主 Agent 定位

主 Agent 是整个系统的业务决策入口和动态控制中枢。它不执行所有具体分析，而是根据任务上下文调用合适的专用 Agent 或工具。

```text
用户 / PDA / Web
        ↓
    CPS 主 Agent
        ↓
读取当前状态、历史记忆、规则和人工反馈
        ↓
提出下一步 Agent / 工具及调用理由
        ↓
人工确认或修改
        ↓
执行被选中的 Agent
        ↓
结果回传主 Agent
        ↓
主 Agent 持续循环决策
```

### 5.2 主 Agent 职责

- 理解用户目标、任务类型和当前巡检状态。
- 读取短期工作记忆、历史案例、规则和人工反馈。
- 判断当前是否具备执行条件。
- 提出下一步 Agent 或工具，并说明调用理由和预期输出。
- 在执行下一步前创建人工调度确认请求。
- 根据人工的批准、改派、跳过、重试或补充指令继续执行。
- 接收结果，更新任务状态，再次决定下一步。
- 在任务完成、证据不足、发生冲突或需要人工处理时结束或转人工。

### 5.3 人工调度确认卡

每一次主 Agent 准备调用下一步 Agent 或工具时，都生成一张调度确认卡。

| 字段 | 说明 |
|---|---|
| 当前状态 | 当前任务已完成的动作和证据完整度 |
| 主 Agent 建议 | 建议调用的 Agent、调用理由、前置条件、预期输出 |
| 候选动作 | 批准、修改 Agent、跳过、重试、退回采集、人工处理 |
| 人工修改 | 最终选择的 Agent、补充指令、修改原因 |
| 确认人 | 用户、角色、时间 |
| 后续结果 | Agent 输出、是否成功、整改效果、是否产生新记忆 |

### 5.4 人工可执行动作

- **批准**：执行主 Agent 推荐的 Agent。
- **修改**：替换主 Agent 推荐的 Agent。
- **跳过**：跳过当前建议，返回主 Agent 重新决策。
- **重试**：使用相同 Agent 重新执行，可补充指令。
- **退回采集**：要求重新上传图片或补充现场信息。
- **人工处理**：由业务人员直接完成当前步骤。
- **补充指令**：为下一步 Agent 增加业务约束或重点关注项。

### 5.5 调度规则

1. 低置信度、证据不足、规则冲突或高风险问题必须请求人工确认。
2. 人工选择优先于主 Agent 建议，但不得绕过权限和数据校验。
3. 人工可以选择候选 Agent 之外的允许 Agent，但不能调用无权限工具。
4. 人工选择和修改必须原样记录，不能只保存最终结果。
5. 主 Agent 因结果不完整再次提出下一步时，必须重新进入人工确认。
6. 写入历史巡检库、发布报告、创建整改任务或发送通知前必须进行权限校验和幂等检查。

## 6. Agent 能力定义

| Agent | 输入 | 输出 |
|---|---|---|
| 问题识别 Agent | 现场图片、产线/区域信息、相似案例 | 问题分类、描述、位置、严重程度、整改建议、证据和置信度 |
| 整改判断 Agent | 整改前后图片、原问题、整改要求、规范案例 | 到位/部分到位/不到位/无法判断、证据、剩余风险和下一步建议 |
| 历史分析 Agent | 历史巡检记录、时间范围、筛选条件 | 问题频率、区域排行、主管排行、复发率、整改措施有效性 |
| 报告 Agent | 已确认问题、整改结论、历史分析、覆盖分析 | HTML 报告草稿、摘要、重点问题、风险和行动建议 |
| 覆盖分析 Agent | 本次覆盖范围、历史高风险范围、采集证据 | 未覆盖场景、数据缺口、风险提示和补充巡检建议 |
| 观察 Agent | 全链路事件、人工调度、Agent 结果、复发和整改效果 | 候选长期记忆、失败模式、流程优化建议和策略评估 |

### 6.1 专用 Agent 约束

- 专用 Agent 只负责本领域任务，不直接改变主流程。
- 专用 Agent 输出必须结构化，包含结果、证据、置信度和异常信息。
- 专用 Agent 不直接写入历史巡检库和生产长期记忆。
- 专用 Agent 可调用的工具由权限系统和 Agent 白名单控制。

## 7. LangChain、LangGraph Runtime 与 DeepAgents 定位

### 7.1 LangChain

LangChain 负责模型和工具能力封装：

- 多模态模型调用。
- 结构化输出和 Pydantic Schema 校验。
- 图片和文本向量检索。
- SQL 查询和统计工具。
- 文件读取和 HTML 模板渲染。
- Agent 工具权限封装。

### 7.2 LangGraph Runtime

LangGraph 不负责预定义业务节点顺序，只作为运行时基础设施：

- 保存 `InspectionState`。
- 保存每次 Agent 调用和人工确认后的 Checkpoint。
- 支持 `interrupt / resume`。
- 支持任务中断、恢复和重试。
- 支持事件流和执行状态查询。
- 保证外部写入操作具备幂等性。

### 7.3 DeepAgents

DeepAgents 用于承载复杂的专用 Agent 或需要多步推理的分析任务，例如：

- 历史趋势分析。
- 报告内容汇总。
- 未覆盖场景分析。
- 复杂整改证据比对。
- 观察链路、总结失败模式和提出优化建议。

## 8. 旁路观察 Agent

### 8.1 产品定位

旁路观察 Agent 是独立于主业务决策链路的监听和分析组件。它不直接控制当前巡检流程，也不直接修改主 Agent 策略，而是监听全链路并提出可审核的记忆和优化建议。

```text
主 Agent / 专用 Agent / 工具 / 人工确认
                    ↓
              事件总线或事件日志
                    ↓
              旁路观察 Agent
                    ↓
       候选记忆 / 失败模式 / 优化建议
                    ↓
                管理员审核
                    ↓
       长期记忆、规则或策略版本库
```

### 8.2 监听范围

- 主 Agent 的任务理解、建议动作和决策理由。
- 人工确认、改派 Agent、跳过、重试、退回及人工补充说明。
- 专用 Agent 的输入、输出、置信度、失败原因和工具调用。
- 报告最终内容与人工修改内容。
- 整改复查结果、后续复发情况和工作安排执行效果。
- 每次 Agent 调度的耗时、失败、重试和人工介入情况。

## 9. 长期记忆与流程优化

### 9.1 可沉淀的记忆类型

| 类型 | 示例 | 用途 |
|---|---|---|
| 业务案例记忆 | 某区域某类问题的最终确认与有效整改方式 | 辅助问题识别和整改建议 |
| 调度偏好记忆 | 某类证据不足时优先调用人工复核 | 优化下一步 Agent 推荐 |
| 人工修正记忆 | 模型常将某现象分类为 A，主管修正为 B | 降低重复误判 |
| 失败模式记忆 | 某类整改图片角度不足导致无法判断 | 生成采集提示和补拍要求 |
| 流程优化记忆 | 报告生成前必须补充某区域数据 | 完善流程检查和覆盖分析 |
| 策略评估记忆 | 某条调用路径后续复发率更低 | 评估是否推荐该路径 |

### 9.2 必须记录的调度记忆

每次人工参与下一步 Agent 调度时，至少记录：

- 当前任务场景和任务目标。
- 主 Agent 推荐的 Agent。
- 主 Agent 推荐理由。
- 人工最终选择的 Agent。
- 人工是否批准、改派、跳过、重试或退回。
- 人工修改原因。
- 人工补充指令。
- 被选 Agent 的实际输出。
- 后续业务结果和整改效果。
- 该决策路径是否有效。

### 9.3 记忆写入原则

1. 观察 Agent 只生成候选记忆，不直接写入生产长期记忆。
2. 候选记忆经过巡检管理员或指定角色审核后才生效。
3. 记忆必须带场景、证据、来源、置信度、创建时间和适用范围。
4. 重复或相互冲突的记忆进入合并或冲突队列。
5. 长期记忆必须支持启用、停用、版本管理、回滚和追溯。
6. 低频、低置信度或没有后续效果验证的经验不能直接升级为强规则。
7. 长期记忆只作为主 Agent 决策参考，不得绕过人工确认机制。

### 9.4 记忆反哺方向

经审核的长期记忆可以反哺：

- 主 Agent 的下一步 Agent 推荐。
- 问题识别 Agent 的相似案例检索。
- 整改判断 Agent 的证据要求和判断依据。
- PDA 的图片采集提示。
- 报告生成前的数据完整性检查。
- 覆盖分析 Agent 的风险区域识别。
- 流程规则和 Agent 工具描述。

## 10. 人工确认与状态管理

| 状态 | 含义 |
|---|---|
| `pending_dispatch_confirmation` | 主 Agent 已提出下一步，等待人工确认 |
| `approved` | 人工同意主 Agent 建议 |
| `modified` | 人工改派 Agent 或补充调用指令 |
| `skipped` | 人工跳过当前建议 |
| `returned` | 人工退回重新采集或整改 |
| `completed` | Agent 执行成功并返回结果 |
| `needs_human` | Agent 无法完成或结果存在冲突，转人工处理 |

## 11. 数据对象

### 11.1 `InspectionCase`

- `inspection_id`
- `line_info`
- `area`
- `supervisor`
- `status`
- `created_by`
- `thread_id`
- `created_at`
- `updated_at`

### 11.2 `AgentDispatch`

- `dispatch_id`
- `inspection_id`
- `recommended_agent`
- `selected_agent`
- `decision`
- `recommendation_reason`
- `operator_id`
- `operator_comment`
- `created_at`

### 11.3 `AgentRun`

- `run_id`
- `dispatch_id`
- `agent_name`
- `input_ref`
- `output`
- `confidence`
- `duration_ms`
- `error`
- `model_version`

### 11.4 `HumanConfirmation`

- `confirmation_id`
- `inspection_id`
- `stage`
- `original_payload`
- `edited_payload`
- `decision`
- `comment`
- `operator_id`
- `confirmed_at`

### 11.5 `MemoryCandidate`

- `memory_id`
- `type`
- `content`
- `condition`
- `evidence`
- `source_events`
- `confidence`
- `review_status`
- `created_at`

### 11.6 `LongTermMemory`

- `memory_id`
- `scope`
- `condition`
- `knowledge`
- `effective`
- `version`
- `reviewer`
- `reviewed_at`
- `rollback_version`

### 11.7 `Report`

- `report_id`
- `inspection_id`
- `draft_data`
- `confirmed_data`
- `html_ref`
- `archive_status`
- `confirmed_by`
- `confirmed_at`

## 12. 产品功能需求

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-01 | 创建巡检任务并维护拉线改造信息 | P0 | 可保存产线、区域、主管和任务状态 |
| FR-02 | 上传并管理现场图片及整改前后证据 | P0 | 图片可关联任务、问题和整改记录 |
| FR-03 | 主 Agent 动态推荐下一步 Agent | P0 | 推荐包含 Agent、理由、前置条件和预期输出 |
| FR-04 | 人工确认下一步 Agent | P0 | 可批准、改派、跳过、重试、退回或补充指令 |
| FR-05 | Agent 结构化输出和结果回传 | P0 | 结果可被主 Agent 读取并用于下一次决策 |
| FR-06 | 生成并编辑巡检报告 | P0 | 报告可预览、修改、确认并导出 HTML |
| FR-07 | 报告转结构化历史数据 | P0 | 确认后才入库，支持幂等和审计 |
| FR-08 | 观察 Agent 监听全链路 | P1 | 可查看事件链和候选优化建议 |
| FR-09 | 长期记忆审核与版本管理 | P1 | 可审核、启用、停用、回滚记忆 |
| FR-10 | 历史分析与覆盖分析 | P1 | 可输出频率、区域、主管、复发和覆盖缺口 |
| FR-11 | 调度记忆查询 | P1 | 主 Agent 可按场景检索历史人工调度经验 |
| FR-12 | 记忆效果评估 | P2 | 可比较记忆启用前后的完成时长、改派率和复发率 |

## 13. 非功能需求

- **可追溯**：所有 Agent 调用、人工决策、数据写入和记忆变更均有事件日志。
- **可靠性**：主 Agent 中断后可从最近一次确认点恢复；重复恢复不重复写库。
- **安全性**：Agent 和工具按角色授权，图片、历史记录和长期记忆隔离访问。
- **可解释性**：每个建议包含证据、理由、置信度和数据来源。
- **可运营性**：支持模型、Agent、工具、记忆和策略的版本化管理。
- **可控性**：线上长期记忆和策略变更必须经过审核，并支持回滚。
- **幂等性**：报告发布、历史入库、任务创建和通知发送必须支持幂等。

## 14. MVP 范围与迭代计划

| 阶段 | 范围 |
|---|---|
| MVP | 任务创建、图片采集、问题识别、人工确认调度、整改判断、人工确认、报告确认、结构化入库 |
| 第二阶段 | 历史分析、覆盖分析、工作安排、观察 Agent、候选记忆审核 |
| 第三阶段 | 记忆效果评估、策略版本管理、自动化复查任务、调度路径优化、质量看板 |

## 15. 关键指标

| 指标 | 定义 |
|---|---|
| 问题识别人工修改率 | 人工修改问题识别结果的任务数 / 触发问题识别的任务数 |
| Agent 改派率 | 人工修改主 Agent 推荐 Agent 的调度次数 / 总调度次数 |
| 人工确认通过率 | 直接批准主 Agent 建议的次数 / 总确认次数 |
| 整改判断准确率 | 人工最终结论与 Agent 结论一致的比例 |
| 记忆采纳率 | 审核后生效的候选记忆数 / 候选记忆总数 |
| 记忆命中率 | 主 Agent 使用长期记忆的有效调度次数 / 使用长期记忆的总调度次数 |
| 流程优化收益 | 优化策略生效后，任务时长、复发率或人工操作次数的变化 |

## 16. 风险与控制措施

| 风险 | 控制措施 |
|---|---|
| 主 Agent 错误选择 Agent | 强制人工调度确认、权限校验和候选 Agent 白名单 |
| 错误记忆污染后续决策 | 候选记忆审核、来源证据、版本控制和回滚 |
| 人工确认过多影响效率 | 按风险和置信度分级，高风险强确认，低风险支持配置批量确认 |
| 统计被模型误解 | 统计由 SQL/代码计算，Agent 只解释并引用指标 |
| 重复写库或重复发通知 | 幂等键、事务状态和事件去重 |
| 观察 Agent 产生错误优化建议 | 建议与线上策略隔离，必须审核后才能生效 |
| Agent 调用链过长 | 设置最大调用轮次、超时、预算和人工接管机制 |

## 17. 待确认事项

1. 人工确认是否严格要求每次调用都确认，还是按风险等级支持可配置豁免。
2. 长期记忆的审核角色、审核时限和失效周期。
3. 可被主 Agent 调用的 Agent 与工具白名单。
4. 历史记录的观察窗口、复发定义和整改有效性计算口径。
5. HTML 报告模板、品牌规范和导出方式。
6. 长期记忆是否区分个人、产线、区域、工厂和全局作用域。
7. 人工改派 Agent 的权限边界和可选范围。
8. 观察 Agent 的运行频率以及实时监听或批量分析模式。
