# CPS 智能巡检系统产品需求文档

> 版本：1.3  
> 初版日期：2026 年 9 月 10 日  
> 文档状态：需求确认稿（仍含明确标注的技术设计及待细化项）；v1.3 已同步评审后的业务决策，新增第 33 节交付边界与验收要求
>
> v1.3 优先规则：本版本更新后的第 21—32 节及第 33 节优先于历史分期和旧建议。全部已确认功能一并交付，兼容旧系统；旧单按旧流程办结、新单走新流程。分类周报无需人工审核，东八区每周一 08:00 自动执行；具体推送渠道由用户实现，本次只预留接口。巡检计划仍须人工批准。
>
> 修订说明：v1.1 新增短长期措施同单互比、整改图片对比、语音录入、分类周报归档、报告驱动巡检计划、辅房拍照点检与评分、物品台账及库存预警。v1.2 调整 CPS 整改流程、Agent 初审规则、巡检事项权限和审核员配置，并精简移动端展示。原有章节保留为基础设计；CPS 流程及展示冲突以第 28—32 节为准，其他扩展规则以第 18—27 节为准。辅房点检规则不因本次 CPS 流程变更而改变。需求确认不代表代码已实现或已通过联调。

## 1. 文档概述

本 PRD 定义 CPS 巡检场景下的业务闭环、主 Agent 动态调度、人工确认机制、旁路观察 Agent 与长期记忆能力。

系统的核心原则是：

> AI 负责提出建议，人工确认或修正下一步动作，系统记录决策并持续优化。

| 项目 | 内容 |
|---|---|
| 产品名称 | CPS 智能巡检系统 |
| 目标用户 | PDA 巡检员工、拉线主管、整改负责人、巡检管理员 |
| 核心场景 | 产线问题发现、整改跟踪、整改复查、报告汇总与持续优化 |
| 技术方向 | LangChain + LangGraph Runtime + DeepAgents + 多模态模型 + 向量库 |
| 本版本目标 | 打通一次巡检从采集到确认、入库和任务生成的可审计闭环 |

## 2. 背景与目标

### 2.1 业务背景

员工通过 PDA 检查产线问题，现场上传图片并跟踪整改。系统需要统一现场信息、历史巡检记录和整改证据，减少人工汇总成本，提升问题识别、整改判断和后续工作安排的质量。

### 2.2 产品目标

1. 将现场图片、拉线改造信息和历史巡检记录沉淀为统一巡检任务。
2. 通过多个专用 Agent 协作完成问题识别、整改判断、历史分析、报告生成和覆盖分析。
3. 每次调用下一步 Agent 前引入人工确认，允许批准、修改、跳过或退回。
4. 将人工调度选择、修改原因和后续结果沉淀为长期记忆，经审核后用于优化未来流程与决策。
5. 所有最终业务事实、报告入库和任务发布均可追溯、可审计。

### 2.3 非目标

1. 本期不自动替代巡检人员对业务结论的最终确认。
2. 本期不允许观察 Agent 直接修改主 Agent 的 Prompt、规则或线上策略。
3. 统计指标由结构化查询和代码计算，模型负责解释和提出建议，不负责伪造统计结果。

## 3. 用户与权限

| 角色 | 主要职责 | 关键权限 |
|---|---|---|
| 巡检员工 | 创建任务、采集图片、补充现场信息 | 提交任务、修改本人未确认内容 |
| 拉线主管 | 确认问题与整改状态、补充整改要求 | 确认、编辑、退回问题和整改结论 |
| 巡检管理员 | 确认报告、入库、审核记忆和优化建议 | 发布报告、审核长期记忆、管理规则 |
| 系统管理员 | 配置模型、Agent、工具和权限 | 配置与审计，不参与业务结论确认 |

## 4. 总体业务流程

系统以 `InspectionCase`（巡检任务）为核心对象。主 Agent 不使用预先固定的业务节点顺序，而是根据当前状态和人工反馈持续提出下一步动作。

```text
创建巡检任务
    ↓
PDA 采集现场信息和问题图片
    ↓
主 Agent 分析当前状态
    ↓
主 Agent 提议下一步 Agent
    ↓
人工确认 / 修改下一步 Agent
    ↓
执行被选中的 Agent
    ↓
结果返回主 Agent
    ↓
主 Agent 再次判断下一步
    ↓
问题确认 → 整改复查 → 报告确认 → 结构化入库
    ↓
生成下一步工作安排
    ↓
观察 Agent 提炼经验并形成候选长期记忆
    ↓
管理员审核记忆并反哺后续决策
```

| 阶段 | 流程 | 人工参与点 | 输出 |
|---|---|---|---|
| 现场采集 | 创建任务 → PDA 上传问题图片 → 补充位置和备注 | 补充或修正现场信息 | 现场问题证据 |
| 问题分析 | 主 Agent 调用问题识别 Agent → 生成候选结论 | 确认、修改、补充或驳回问题 | 已确认问题 |
| 整改复查 | 提交整改后图片 → 主 Agent 调用整改判断 Agent | 确认是否执行判断、确认整改结论 | 最终整改状态 |
| 报告闭环 | 调用历史分析、覆盖分析、报告 Agent | 确认报告、确认入库 | HTML 报告与结构化数据 |
| 持续优化 | 观察 Agent 监听链路 → 筛选记忆 → 审核后入库 | 审核记忆与优化建议 | 长期记忆与优化策略 |

## 5. 主 Agent 动态调度

### 5.1 主 Agent 定位

主 Agent 是整个系统的业务决策入口和动态控制中枢。它不执行所有具体分析，而是根据任务上下文调用合适的专用 Agent 或工具。

```text
用户 / PDA / Web
        ↓
    CPS 主 Agent
        ↓
读取当前状态、历史记忆、规则和人工反馈
        ↓
提出下一步 Agent / 工具及调用理由
        ↓
人工确认或修改
        ↓
执行被选中的 Agent
        ↓
结果回传主 Agent
        ↓
主 Agent 持续循环决策
```

### 5.2 主 Agent 职责

- 理解用户目标、任务类型和当前巡检状态。
- 读取短期工作记忆、历史案例、规则和人工反馈。
- 判断当前是否具备执行条件。
- 提出下一步 Agent 或工具，并说明调用理由和预期输出。
- 在执行下一步前创建人工调度确认请求。
- 根据人工的批准、改派、跳过、重试或补充指令继续执行。
- 接收结果，更新任务状态，再次决定下一步。
- 在任务完成、证据不足、发生冲突或需要人工处理时结束或转人工。

### 5.3 人工调度确认卡

每一次主 Agent 准备调用下一步 Agent 或工具时，都生成一张调度确认卡。

| 字段 | 说明 |
|---|---|
| 当前状态 | 当前任务已完成的动作和证据完整度 |
| 主 Agent 建议 | 建议调用的 Agent、调用理由、前置条件、预期输出 |
| 候选动作 | 批准、修改 Agent、跳过、重试、退回采集、人工处理 |
| 人工修改 | 最终选择的 Agent、补充指令、修改原因 |
| 确认人 | 用户、角色、时间 |
| 后续结果 | Agent 输出、是否成功、整改效果、是否产生新记忆 |

### 5.4 人工可执行动作

- **批准**：执行主 Agent 推荐的 Agent。
- **修改**：替换主 Agent 推荐的 Agent。
- **跳过**：跳过当前建议，返回主 Agent 重新决策。
- **重试**：使用相同 Agent 重新执行，可补充指令。
- **退回采集**：要求重新上传图片或补充现场信息。
- **人工处理**：由业务人员直接完成当前步骤。
- **补充指令**：为下一步 Agent 增加业务约束或重点关注项。

### 5.5 调度规则

1. 低置信度、证据不足、规则冲突或高风险问题必须请求人工确认。
2. 人工选择优先于主 Agent 建议，但不得绕过权限和数据校验。
3. 人工可以选择候选 Agent 之外的允许 Agent，但不能调用无权限工具。
4. 人工选择和修改必须原样记录，不能只保存最终结果。
5. 主 Agent 因结果不完整再次提出下一步时，必须重新进入人工确认。
6. 写入历史巡检库、发布报告、创建整改任务或发送通知前必须进行权限校验和幂等检查。

## 6. Agent 能力定义

| Agent | 输入 | 输出 |
|---|---|---|
| 问题识别 Agent | 现场图片、产线/区域信息、相似案例 | 问题分类、描述、位置、严重程度、整改建议、证据和置信度 |
| 整改判断 Agent | 整改前后图片、原问题、整改要求、规范案例 | 到位/部分到位/不到位/无法判断、证据、剩余风险和下一步建议 |
| 历史分析 Agent | 历史巡检记录、时间范围、筛选条件 | 问题频率、区域排行、主管排行、复发率、整改措施有效性 |
| 报告 Agent | 已确认问题、整改结论、历史分析、覆盖分析 | HTML 报告草稿、摘要、重点问题、风险和行动建议 |
| 覆盖分析 Agent | 本次覆盖范围、历史高风险范围、采集证据 | 未覆盖场景、数据缺口、风险提示和补充巡检建议 |
| 观察 Agent | 全链路事件、人工调度、Agent 结果、复发和整改效果 | 候选长期记忆、失败模式、流程优化建议和策略评估 |

### 6.1 专用 Agent 约束

- 专用 Agent 只负责本领域任务，不直接改变主流程。
- 专用 Agent 输出必须结构化，包含结果、证据、置信度和异常信息。
- 专用 Agent 不直接写入历史巡检库和生产长期记忆。
- 专用 Agent 可调用的工具由权限系统和 Agent 白名单控制。

## 7. LangChain、LangGraph Runtime 与 DeepAgents 定位

### 7.1 LangChain

LangChain 负责模型和工具能力封装：

- 多模态模型调用。
- 结构化输出和 Pydantic Schema 校验。
- 图片和文本向量检索。
- SQL 查询和统计工具。
- 文件读取和 HTML 模板渲染。
- Agent 工具权限封装。

### 7.2 LangGraph Runtime

LangGraph 不负责预定义业务节点顺序，只作为运行时基础设施：

- 保存 `InspectionState`。
- 保存每次 Agent 调用和人工确认后的 Checkpoint。
- 支持 `interrupt / resume`。
- 支持任务中断、恢复和重试。
- 支持事件流和执行状态查询。
- 保证外部写入操作具备幂等性。

### 7.3 DeepAgents

DeepAgents 用于承载复杂的专用 Agent 或需要多步推理的分析任务，例如：

- 历史趋势分析。
- 报告内容汇总。
- 未覆盖场景分析。
- 复杂整改证据比对。
- 观察链路、总结失败模式和提出优化建议。

## 8. 旁路观察 Agent

### 8.1 产品定位

旁路观察 Agent 是独立于主业务决策链路的监听和分析组件。它不直接控制当前巡检流程，也不直接修改主 Agent 策略，而是监听全链路并提出可审核的记忆和优化建议。

```text
主 Agent / 专用 Agent / 工具 / 人工确认
                    ↓
              事件总线或事件日志
                    ↓
              旁路观察 Agent
                    ↓
       候选记忆 / 失败模式 / 优化建议
                    ↓
                管理员审核
                    ↓
       长期记忆、规则或策略版本库
```

### 8.2 监听范围

- 主 Agent 的任务理解、建议动作和决策理由。
- 人工确认、改派 Agent、跳过、重试、退回及人工补充说明。
- 专用 Agent 的输入、输出、置信度、失败原因和工具调用。
- 报告最终内容与人工修改内容。
- 整改复查结果、后续复发情况和工作安排执行效果。
- 每次 Agent 调度的耗时、失败、重试和人工介入情况。

## 9. 长期记忆与流程优化

### 9.1 可沉淀的记忆类型

| 类型 | 示例 | 用途 |
|---|---|---|
| 业务案例记忆 | 某区域某类问题的最终确认与有效整改方式 | 辅助问题识别和整改建议 |
| 调度偏好记忆 | 某类证据不足时优先调用人工复核 | 优化下一步 Agent 推荐 |
| 人工修正记忆 | 模型常将某现象分类为 A，主管修正为 B | 降低重复误判 |
| 失败模式记忆 | 某类整改图片角度不足导致无法判断 | 生成采集提示和补拍要求 |
| 流程优化记忆 | 报告生成前必须补充某区域数据 | 完善流程检查和覆盖分析 |
| 策略评估记忆 | 某条调用路径后续复发率更低 | 评估是否推荐该路径 |

### 9.2 必须记录的调度记忆

每次人工参与下一步 Agent 调度时，至少记录：

- 当前任务场景和任务目标。
- 主 Agent 推荐的 Agent。
- 主 Agent 推荐理由。
- 人工最终选择的 Agent。
- 人工是否批准、改派、跳过、重试或退回。
- 人工修改原因。
- 人工补充指令。
- 被选 Agent 的实际输出。
- 后续业务结果和整改效果。
- 该决策路径是否有效。

### 9.3 记忆写入原则

1. 观察 Agent 只生成候选记忆，不直接写入生产长期记忆。
2. 候选记忆经过巡检管理员或指定角色审核后才生效。
3. 记忆必须带场景、证据、来源、置信度、创建时间和适用范围。
4. 重复或相互冲突的记忆进入合并或冲突队列。
5. 长期记忆必须支持启用、停用、版本管理、回滚和追溯。
6. 低频、低置信度或没有后续效果验证的经验不能直接升级为强规则。
7. 长期记忆只作为主 Agent 决策参考，不得绕过人工确认机制。

### 9.4 记忆反哺方向

经审核的长期记忆可以反哺：

- 主 Agent 的下一步 Agent 推荐。
- 问题识别 Agent 的相似案例检索。
- 整改判断 Agent 的证据要求和判断依据。
- PDA 的图片采集提示。
- 报告生成前的数据完整性检查。
- 覆盖分析 Agent 的风险区域识别。
- 流程规则和 Agent 工具描述。

## 10. 人工确认与状态管理

| 状态 | 含义 |
|---|---|
| `pending_dispatch_confirmation` | 主 Agent 已提出下一步，等待人工确认 |
| `approved` | 人工同意主 Agent 建议 |
| `modified` | 人工改派 Agent 或补充调用指令 |
| `skipped` | 人工跳过当前建议 |
| `returned` | 人工退回重新采集或整改 |
| `completed` | Agent 执行成功并返回结果 |
| `needs_human` | Agent 无法完成或结果存在冲突，转人工处理 |

## 11. 数据对象

### 11.1 `InspectionCase`

- `inspection_id`
- `line_info`
- `area`
- `supervisor`
- `status`
- `created_by`
- `thread_id`
- `created_at`
- `updated_at`

### 11.2 `AgentDispatch`

- `dispatch_id`
- `inspection_id`
- `recommended_agent`
- `selected_agent`
- `decision`
- `recommendation_reason`
- `operator_id`
- `operator_comment`
- `created_at`

### 11.3 `AgentRun`

- `run_id`
- `dispatch_id`
- `agent_name`
- `input_ref`
- `output`
- `confidence`
- `duration_ms`
- `error`
- `model_version`

### 11.4 `HumanConfirmation`

- `confirmation_id`
- `inspection_id`
- `stage`
- `original_payload`
- `edited_payload`
- `decision`
- `comment`
- `operator_id`
- `confirmed_at`

### 11.5 `MemoryCandidate`

- `memory_id`
- `type`
- `content`
- `condition`
- `evidence`
- `source_events`
- `confidence`
- `review_status`
- `created_at`

### 11.6 `LongTermMemory`

- `memory_id`
- `scope`
- `condition`
- `knowledge`
- `effective`
- `version`
- `reviewer`
- `reviewed_at`
- `rollback_version`

### 11.7 `Report`

- `report_id`
- `inspection_id`
- `draft_data`
- `confirmed_data`
- `html_ref`
- `archive_status`
- `confirmed_by`
- `confirmed_at`

## 12. 产品功能需求

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-01 | 创建巡检任务并维护拉线改造信息 | P0 | 可保存产线、区域、主管和任务状态 |
| FR-02 | 上传并管理现场图片及整改前后证据 | P0 | 图片可关联任务、问题和整改记录 |
| FR-03 | 主 Agent 动态推荐下一步 Agent | P0 | 推荐包含 Agent、理由、前置条件和预期输出 |
| FR-04 | 人工确认下一步 Agent | P0 | 可批准、改派、跳过、重试、退回或补充指令 |
| FR-05 | Agent 结构化输出和结果回传 | P0 | 结果可被主 Agent 读取并用于下一次决策 |
| FR-06 | 生成并编辑巡检报告 | P0 | 报告可预览、修改、确认并导出 HTML |
| FR-07 | 报告转结构化历史数据 | P0 | 确认后才入库，支持幂等和审计 |
| FR-08 | 观察 Agent 监听全链路 | P1 | 可查看事件链和候选优化建议 |
| FR-09 | 长期记忆审核与版本管理 | P1 | 可审核、启用、停用、回滚记忆 |
| FR-10 | 历史分析与覆盖分析 | P1 | 可输出频率、区域、主管、复发和覆盖缺口 |
| FR-11 | 调度记忆查询 | P1 | 主 Agent 可按场景检索历史人工调度经验 |
| FR-12 | 记忆效果评估 | P2 | 可比较记忆启用前后的完成时长、改派率和复发率 |

## 13. 非功能需求

- **可追溯**：所有 Agent 调用、人工决策、数据写入和记忆变更均有事件日志。
- **可靠性**：主 Agent 中断后可从最近一次确认点恢复；重复恢复不重复写库。
- **安全性**：Agent 和工具按角色授权，图片、历史记录和长期记忆隔离访问。
- **可解释性**：每个建议包含证据、理由、置信度和数据来源。
- **可运营性**：支持模型、Agent、工具、记忆和策略的版本化管理。
- **可控性**：线上长期记忆和策略变更必须经过审核，并支持回滚。
- **幂等性**：报告发布、历史入库、任务创建和通知发送必须支持幂等。

## 14. MVP 范围与迭代计划

| 阶段 | 范围 |
|---|---|
| MVP | 任务创建、图片采集、问题识别、人工确认调度、整改判断、人工确认、报告确认、结构化入库 |
| 第二阶段 | 历史分析、覆盖分析、工作安排、观察 Agent、候选记忆审核 |
| 第三阶段 | 记忆效果评估、策略版本管理、自动化复查任务、调度路径优化、质量看板 |

## 15. 关键指标

| 指标 | 定义 |
|---|---|
| 问题识别人工修改率 | 人工修改问题识别结果的任务数 / 触发问题识别的任务数 |
| Agent 改派率 | 人工修改主 Agent 推荐 Agent 的调度次数 / 总调度次数 |
| 人工确认通过率 | 直接批准主 Agent 建议的次数 / 总确认次数 |
| 整改判断准确率 | 人工最终结论与 Agent 结论一致的比例 |
| 记忆采纳率 | 审核后生效的候选记忆数 / 候选记忆总数 |
| 记忆命中率 | 主 Agent 使用长期记忆的有效调度次数 / 使用长期记忆的总调度次数 |
| 流程优化收益 | 优化策略生效后，任务时长、复发率或人工操作次数的变化 |

## 16. 风险与控制措施

| 风险 | 控制措施 |
|---|---|
| 主 Agent 错误选择 Agent | 强制人工调度确认、权限校验和候选 Agent 白名单 |
| 错误记忆污染后续决策 | 候选记忆审核、来源证据、版本控制和回滚 |
| 人工确认过多影响效率 | 按风险和置信度分级，高风险强确认，低风险支持配置批量确认 |
| 统计被模型误解 | 统计由 SQL/代码计算，Agent 只解释并引用指标 |
| 重复写库或重复发通知 | 幂等键、事务状态和事件去重 |
| 观察 Agent 产生错误优化建议 | 建议与线上策略隔离，必须审核后才能生效 |
| Agent 调用链过长 | 设置最大调用轮次、超时、预算和人工接管机制 |

## 17. 待确认事项

1. 人工确认是否严格要求每次调用都确认，还是按风险等级支持可配置豁免。
2. 长期记忆的审核角色、审核时限和失效周期。
3. 可被主 Agent 调用的 Agent 与工具白名单。
4. 历史记录的观察窗口、复发定义和整改有效性计算口径。
5. HTML 报告模板、品牌规范和导出方式。
6. 长期记忆是否区分个人、产线、区域、工厂和全局作用域。
7. 人工改派 Agent 的权限边界和可选范围。
8. 观察 Agent 的运行频率以及实时监听或批量分析模式。

## 18. 本轮需求范围与确认基线

### 18.1 文档约定

- **已确认**：用户明确提出或在本轮讨论中确认的业务规则。
- **建议**：为实现闭环提出的设计方案，尚未视为业务批准。
- **待确认**：影响实现、操作或验收口径，需进一步决策。
- 第 17 节保留的是原有 Agent 框架相关待确认项；本轮新增模块的待确认项集中在第 27 节。
- 本轮仅形成 PRD，不授权开发、部署或对生产数据进行操作。

### 18.2 功能范围

| 编号 | 功能 | 确认范围 |
|---|---|---|
| FR-13 | 短长期措施及雷同性审核 | 措施拆分为短期、长期；仅对同一问题单的两类措施互比，不包含跨历史措施审核 |
| FR-14 | 整改图片比对 | Agent 使用视觉大模型对比整改前后图片，判断整改效果 |
| FR-15 | 语音转文字 | 多文字输入场景支持语音输入，转写用于文字录入 |
| FR-16 | 分类巡检周报 | 各类巡检单独出报告，每周定期生成并推送；文件上传存储，保留运行记录和文件地址，管理端查询下载 |
| FR-17 | 报告驱动巡检计划 | 根据报告生成计划，人工审核后自动触发区域巡检、辅房点检、整改复查任务 |
| FR-18 | 辅房基础资料及点检配置 | 维护辅房档案和可配置点检项 |
| FR-19 | 辅房视觉点检 | 全部以拍照为判定依据；先校验图片与类型匹配，再按点检内容判断 |
| FR-20 | 点检确认与房间排名 | 本人提交前可修改判定结果，提交后锁定；每房间初始 100 分，不合格扣对应分值 |
| FR-21 | 辅房物品台账及出入库 | 维护物品、位置、责任人、库存与出入库流水 |
| FR-22 | 库存预警 | 库存数量小于等于预警数量时触发预警 |

### 18.3 本轮替换和排除的方案

1. 辅房语音文件上传并判定点检结果的早期方案取消；语音只作为文字录入辅助，不能替代照片证据。
2. 房间排名不使用此前建议的“适用项得分率”，改用 100 分起始扣分制。
3. 不把图片类型不符、无法判断直接当作现场不合格扣分；必须补拍且不得绕过照片要求提交。
4. 不默认提供提交后管理员修改、撤回或重新打开入口。
5. 报告文件上传、管理端查询下载不替代每周推送；推送渠道等细节另行确定。

## 19. CPS 短长期措施与雷同性审核

### 19.1 已确认需求

- 将问题单原措施拆分为短期措施、长期措施两个字段。
- Agent 对同单短期措施和长期措施进行语义雷同性审核。
- 不纳入跨单、跨人员或跨历史措施比对。

### 19.2 建议的业务解释及输出

短期措施用于处理当前问题与即时风险；长期措施用于防止复发和改善根因。审核不仅比较文字相似，还应识别不同措辞是否实际表达同一措施。

建议输出：审核意见、两类措施的相似片段、实质差异、判断理由和修改建议。标准措施合理复用不能仅凭相似度认定违规。审核不自动替代人工处置。

最新确认：整改人员提交后统一进行 Agent 初审；审核意见交审核专员裁决，发现问题不自动退回。原因、短期措施和长期措施分别不得少于 15 字，标点比例不超过 10%，详见第 28—29 节。雷同性阈值与具体语义评价标准仍待确定；接受并说明原因、标记误判等额外操作仅为建议。

## 20. 整改前后图片比对及语音录入

### 20.1 整改效果判断

**已确认**：Agent 调用视觉大模型，对整改前后图片进行比对，确认整改效果。

**建议规则**：

- 以具体问题、整改要求和关联的前后图片为输入，不以图片是否变化替代是否整改到位。
- 输出整改到位、部分到位、未到位、无法判断，并提供理由和证据引用。
- 图片模糊、对象不一致、关键区域遮挡或证据缺失时，不默认通过。
- 无法凭照片验证的性能或制度落实情况，提示补充证据或人工核查。
- 沿用 CPS 现有人工关闭或退回流程，不因模型判断通过直接关闭业务问题。

### 20.2 通用语音转文字

**已确认**：多文字输入场景支持语音转文字，不作为辅房点检判断证据。

**建议覆盖字段**：问题描述、原因分析、短期措施、长期措施、整改说明、审核意见及点检备注。

**建议交互**：录音 → 转写 → 用户编辑确认 → 提交到对应文本字段；转写失败允许重试或手工输入。具体页面、终端支持、录音保存及保留周期待确认。

## 21. 巡检周报 Agent、Skill 与报告归档

### 21.1 已确认需求

1. 每周定期生成并推送巡检报告，各类巡检单独出报告，不混合成一份。
2. 报告生成后上传至文件存储。
3. 持久保存报告运行记录，关联报告文件地址。
4. 管理端能够查询记录并下载对应报告。
5. 使用可复用报告 Skill 承载报告模板、统计口径、分析方法和质量检查，由报告 Agent 调用；定时执行、存储和推送由系统负责。
6. 东八区（UTC+8）每周一上午 08:00 自动执行，统计上一个完整自然周：[上周一 00:00，本周一 00:00)，不含结束时刻。
7. 周报自动生成、上传归档并进入推送流程，无需人工审核。这是定期周报的明确例外，不取消巡检计划审核，也不自动取消其他报告场景已有审核要求。
8. 推送仅预留可接入代码接口，由用户实现具体渠道；未接入时必须显示“推送未配置”，不得记为发送成功。报告生成、存储、运行记录和管理端下载仍须完整交付。

### 21.2 建议的 Agent 与 Skill 分工

| 组件 | 职责 |
|---|---|
| 报告 Agent | 组织业务数据、分析问题与趋势、生成报告草稿，指出信息缺口 |
| 报告 Skill | 复用报告结构、统计口径、分析方法、引用规则及质量检查规范 |
| 业务系统和运行时 | 数据访问与权限、执行调度、存储、运行记录、归档、下载和推送 |

Skill 不等于独立 Agent 服务，不自动获得数据权限或定时执行能力。优先增强现有报告 Agent，不能将“已有角色”视为业务端完整接通。

### 21.3 建议报告内容与运行记录

建议报告包含统计周期、执行情况、问题及风险、整改进度、重复发生情况、未完成事项、覆盖缺口和后续建议。辅房报告增加房间得分与排名。库存是否纳入报告范围待确认。

建议运行记录字段：运行编号、巡检类型、统计周期、开始时间、结束时间、执行状态、失败阶段及原因、文件名称、文件存储地址、关联计划与推送状态。

建议区分生成中、生成失败、上传中、上传失败、文件已归档，以及独立的推送状态。文件生成但上传失败不得展示为已归档；上传成功不等于推送成功。

建议管理端按巡检类型、周期和运行状态查询，查看失败原因并下载有权限访问的文件。文件地址应是受控存储引用，不要求公开访问链接。

### 21.4 待确认边界

执行时间、时区、统计周期和无需审核已按第 21.1 节确认。推送接口及未配置状态属于本次交付，具体渠道与接收人由用户接入实现，不再作为本次真实发送的验收前提。巡检分类清单、文件格式、重跑与文件版本保留策略仍需细化；不能以旧章节的人工归档要求重新给分类周报增加人工审批门槛。

## 22. 巡检计划 Agent 与业务任务触发

### 22.1 已确认需求

- 依据巡检报告生成巡检计划。
- 计划须经人工审核后，才能自动触发业务巡检任务。
- 支持区域巡检、辅房点检、整改复查三类任务。
- 任务生成与任务执行完成是不同状态，不能创建后直接视为完成。

### 22.2 建议的计划结构与控制

计划建议包含来源报告、目标及风险依据、任务类型、巡检对象、点检内容、负责人、时间、频次、验收标准及证据要求。

建议流程：报告形成 → 计划 Agent 生成草稿 → 人工调整和审核 → 根据批准规则生成任务 → 人员执行 → 提交并汇总结果。

**已确认执行规则**：人工审核时确定每项任务的负责人及执行时间；批准后立即创建区域巡检、辅房点检、整改复查对应业务任务，任务按计划时间执行。创建失败只补建未成功项，不重复生成已成功的任务；不把计划执行时间误作为延迟建单时间。

以下保留为控制设计建议：

- 以批准版本、触发批次和目标对象进行幂等控制，重复执行不重复建单。
- 明确负责人路由；无法派发时进入待分配或错误状态，不静默丢失。
- 计划重大变更重新审核；撤销后停止未来触发，已生成任务单独处置。
- 保留审核、触发、生成成功或失败及关联任务记录。

## 23. 辅房资料与点检项配置

### 23.1 辅房原始字段（已确认）

| 字段 | 含义 |
|---|---|
| 楼栋 | 辅房所在楼栋 |
| 门牌 | 辅房门牌信息 |
| 名称 | 辅房名称 |
| 类型 | 辅房类别 |
| 风险等级 | 辅房风险分类 |
| 部门 | 所属部门 |
| 责任人工号 | 辅房责任人员工号 |
| 责任人姓名 | 辅房责任人姓名 |
| 责任人经理工号 | 责任人经理员工号 |
| 责任人经理姓名 | 责任人经理姓名 |

建议补充唯一编号和启用状态；基地、工厂是否作为辅房字段以关联库存位置待确认。

### 23.2 点检项原始字段（已确认）

| 字段 | 规则 |
|---|---|
| 内容 | 视觉模型需要判断的具体检查要求，如地面无积水、无油污、无杂物 |
| 类型 | 图片应包含的检查对象类别，如地面；与辅房类型是不同概念 |
| 分数 | 本项不合格时应扣除的分值，不是实得分或权重 |
| 状态 | 适用／不适用，表示该项是否需要检查，不等于执行结果 |

建议增加点检项编号、适用辅房范围和配置版本。执行记录引用检查时配置，避免后续修改规则改变历史解释。

## 24. 辅房视觉判定、提交锁定与房间评分

### 24.1 已确认判定流程

选择点检项 → 拍照 → Agent 调用视觉大模型校验对象类型 → 按内容判断 → 点检人确认或修改 → 提交锁定 → 计分与排名。

所有辅房点检均以拍照作为判定依据，不允许语音替代图片。模型需要同时读取该项类型和内容，不能只识别照片主体而忽略具体检查要求。

| 阶段 | 判断要求 | 结果与处理 |
|---|---|---|
| 类型匹配 | 照片内容是否符合点检项类型 | 不匹配则必须重拍，不进入合格判断 |
| 内容判断 | 符合类型的图片是否满足点检内容 | 返回合格或不合格，并给出理由 |
| 证据可用性 | 是否模糊、遮挡或缺少必要区域 | 无法判断则补拍，不默认通过 |

例：类型为“地面”、内容为“无积水、无油污、无杂物”、分数为 10。桌面照片类型不符，必须重拍；地面有积水则不合格，扣 10 分；满足全部要求则合格，不扣分；图片模糊则无法判断，补拍。

### 24.2 已确认修改及提交规则

1. 以登录身份识别实际移动端点检人，不以设备本身作为身份。
2. 点检人仅能在本人点检单提交前修改判定结果；有效图片对应的合格、不合格结果均可在提交前确认或修正。
3. 类型不符或无法判断不等于现场不合格，不直接扣分；必须补拍后才能提交，不允许手工改成合格绕过证据要求。
4. 提交后结果锁定，点检人无法继续修改；本期不默认增加管理员修改、撤回或重开入口。
5. 建议保留 AI 原始结果、图片引用、人工修改前后结果、修改人、修改时间和原因，以便追溯。
6. 提交前结果修改后，按最终结果重新计算当前得分。

### 24.3 已确认评分与排名

- 每个房间每次点检初始满分 100 分，单次最终得分 = max(0, 100 − 不合格适用项扣分值之和)。
- 合格项、不适用项均不扣分；有效结果经点检人修改后，以提交时最终结果计算。
- 每次检查时间与该次得分分别存储、分别展示，但关联同一条点检记录；不能用一个房间总分覆盖历史单次结果。
- 周报排名周期为东八区上一完整自然周，与第 21.1 节一致。
- 周期总分为周期内该房间已提交有效点检记录的单次得分之和，按总分从高到低排名；不取平均分、最低分或最新一次得分，也不按适用项满分比例折算。
- 报告必须展示房间、逐次检查时间、单次得分、不合格项与扣分明细、周期检查次数、周期总分及排名。
- 检查次数会影响总分排名，用户已确认接受该口径；报告展示次数以帮助解释。
- 未完成或证据无效的任务不得按满分已完成记录计入总分。

**待细化边界**：排名对象的组织范围、同分显示规则，以及跨周补提交记录按检查时间还是提交时间划入周期；不得以这些细节未定改用均分或最新一次得分。

## 25. 辅房物品台账、出入库与库存预警

### 25.1 台账原始字段（已确认）

| 字段 | 含义 |
|---|---|
| 物品名称 | 台账物品名称 |
| 计量单位 | 库存和出入库数量使用的单位 |
| 库存数量 | 当前库存数量 |
| 预警数量 | 触发库存预警的阈值 |
| 基地 | 所属基地 |
| 工厂 | 所属工厂 |
| 存储房间 | 物品存放房间 |
| 房间责任人工号 | 房间责任人员工号 |
| 房间责任人姓名 | 房间责任人姓名 |

### 25.2 已确认范围

- 提供辅房物品台账管理。
- 包含出入库流水，不能只维护一个可覆盖的库存数字而不记录出入库。
- 库存数量 **小于等于预警数量** 时触发库存预警，等于阈值也必须预警。

### 25.3 建议控制规则

- 台账支持新增、编辑、查询；入库增加库存，出库减少库存。
- 流水建议记录物品、房间、出入库类型、数量、单位、变更前后库存、操作人、操作时间及说明。
- 同一台账使用统一单位；单位换算、退回及盘点调整范围另行确认。
- **已确认**：禁止超可用库存出库、禁止负库存。
- **已确认**：预警通知房间责任人；库存大于阈值时自动解除预警。同一物品同一房间持续不足期间合并为同一预警事件，恢复后再次不足形成新事件。
- 盘点调整单独留痕、不直接覆盖流水为设计建议。库存通知渠道、频次、升级规则及确认角色待细化；周报推送接口由用户实现的边界，不自动扩大为取消库存通知需求。

## 26. 实现边界与验收清单

### 26.1 与现有系统关系

- `cps` 承载 Java 巡检业务、员工移动端、管理台及知识检索；`basic-project` 承载 Agent 框架与独立工作台。
- 既有报告、工作计划和整改判断 Agent 优先增强复用，报告 Skill 具体实现方式待技术设计。
- 当前 Java 创建问题时已具备创建 Agent 巡检并启动的适配入口，但不能据此认定整改同步、结果回写或计划到业务任务已完整接通。
- 文件存储可评估复用现有 RustFS；使用已有存储组件不等于报告上传、运行记录和下载功能已经实现。
- 附件传递可靠性和真实接口联调是前置核验事项；不能以移动端 mock 回退结果作为验收证据。

### 26.2 已确认需求验收标准

| 编号 | 验收标准 |
|---|---|
| AC-01 | 同一问题单能够分别保存短期与长期措施，Agent 的雷同性审核对象仅为该单两类措施 |
| AC-02 | 可将整改前后图片送入视觉模型比对并返回整改效果判断；真实输入和结果能对应到同一业务问题 |
| AC-03 | 约定的长文本场景支持语音转文字；辅房判定链路不能把语音作为照片的替代证据 |
| AC-04 | 每周按巡检类别分别生成报告；每份报告上传文件存储，运行记录关联文件地址，管理端能够查询并下载 |
| AC-05 | 东八区每周一 08:00 自动汇总上一完整自然周，各类周报无需审核即可生成归档；预留推送接口供用户实现渠道，未接入时显示“推送未配置”，不能将上传成功标为发送成功；不以真实渠道发送作为本次交付条件 |
| AC-06 | 根据报告形成计划，未经人工审核不生成业务巡检任务；批准后支持区域巡检、辅房点检、整改复查三类任务 |
| AC-07 | 辅房与点检项能够维护第 23 节全部原始字段；类型与内容共同进入视觉判断输入 |
| AC-08 | 地面点检上传桌面图片时明确提示类型不符，要求重拍，不能扣分或手动改合格后绕过提交 |
| AC-09 | 图片类型匹配后按具体点检内容返回判断；模糊或遮挡无法判定时要求补拍且阻止提交 |
| AC-10 | 本人点检单提交前可修正有效图片的判断并重新计分；其他人不能修改该单结果；提交后禁止继续修改 |
| AC-11 | 房间从 100 分起算，例如两个不合格项扣分值分别为 10、15，结果为 75 分，合格项不扣分；结果用于排名 |
| AC-12 | 物品台账包含第 25 节全部原始字段，入库、出库形成可查询流水并正确增减库存 |
| AC-13 | 预警阈值为 5 时，库存 4 和 5 均触发低库存条件，库存 6 不满足低库存条件 |

视觉准确率、报告质量和语音转写效果需使用业务确认样本评估；不在未评估前虚设准确率承诺。建议测试合格、不合格、对象不符、模糊遮挡、人工修正及失败恢复等场景。

## 27. 剩余待确认事项

下列事项未获业务明确确认，不因写入 PRD 自动成为已批准规则：

1. 同单措施雷同性阈值及无意义、敷衍文字的业务样例；触发时机、人工裁决角色、三个文本字段的长度及标点规则已在第 28—29 节确认，不再作为待确认项。
2. 整改效果输出枚举及补证方式；人工审核通过直接关闭、不通过退回整改人员已确认。
3. 语音录入覆盖页面和终端、支持语言、录音时长、是否保存原音及保留周期。
4. 周报巡检分类清单、文件格式、重跑及版本保留规则。每周一 08:00 东八区、上一自然周统计、无需审核已确认；具体推送渠道及接收人由用户实现，交付边界见第 21 节。
5. 巡检计划变更、撤销和已生成任务处置方式。审核时确定负责人及时间、批准后立即建单、按计划时间执行、仅补建失败项已确认。
6. 房间排名组织范围、同分处理和跨周补提交的时间归属。单次 0 分封底、不适用不扣分、自然周已提交有效记录总分排名及展示逐次记录已确认。
7. 辅房是否补充基地、工厂，以及点检项与不同辅房的配置关联方式。
8. 库存初始化来源、盘点/退回/单位换算范围、库存通知渠道及频次。禁止负库存、通知房间责任人、持续不足合并预警、大于阈值自动解除已确认。
9. 各模块页面操作权限、视觉模型业务样本和效果验收指标；角色权限不得默认扩大提交后修改权。

## 28. CPS 问题整改流程调整（v1.2 已确认）

### 28.1 新业务流程

```text
问题稽查人填写问题
    ↓
整改人员回复原因、短期措施、长期措施
选择责任员工、上传整改照片（支持暂存和转办）
    ↓ 提交
Agent 初审：前后图片、措施雷同性、文本有效性、字数及标点比例
    ↓ 初审发现问题仍交人工决定
审核专员查看 AI 初审意见及整改内容
    ├─ 通过 → 直接关闭问题
    └─ 不通过 → 退回整改人员
```

- 原先分开的反馈、责任人整改、凭证上传节点，收敛为本节整改人员处理节点。
- 责任员工仅作为选择和责任记录，没有独立操作节点；不得因选择责任员工自动增加待办流转节点。
- 整改人员可暂存内容，并支持转办。
- AI 只提供初审意见，不拥有最终关闭或退回决定权；初审发现问题不能自动退回或自动关闭。
- 审核专员审核通过直接关闭问题，不通过退回整改人员。
- 本流程仅针对 CPS 问题整改；辅房点检的本人提交前修改、提交后锁定规则保持不变。

### 28.2 整改处理内容

| 内容 | 已确认要求 |
|---|---|
| 原因 | 由整改人员填写，纳入文本有效性、字数及标点审核 |
| 短期措施 | 单独填写，与同单长期措施比对，并纳入文本审核 |
| 长期措施 | 单独填写，与同单短期措施比对，并纳入文本审核 |
| 责任员工 | 选择并记录，不产生独立操作节点 |
| 整改照片 | 上传作为整改后证据，参与前后图片对比 |
| 暂存 | 支持保存整改办理中的内容 |
| 转办 | 支持将整改办理工作转交其他人员 |

### 28.3 已确认编辑、转办和退回规则

- 仅整改办理中、退回整改时允许编辑或转办；提交后锁定当前提交版本，AI 初审中及待人工审核时不得继续修改或转办该版本。
- 转办后仅当前承办人办理并接收审核退回，原办理人不再具有该单办理权限。
- 转办不自动改变所选责任员工；责任员工仍无独立操作节点。
- 退回后修改并重新提交，必须再次触发 AI 初审，不能沿用上一版本意见。
- 初审结果关联提交版本及对应证据，旧版本结果不能覆盖新版本或推进其流程。
- 暂存允许不完整内容且不触发初审、转办保存历史内容与记录，为设计建议；转办目标范围、是否跨厂区仍待细化。

### 28.4 已确认 AI 超时与人工接管

- 以本次初审任务提交成功时刻开始计时，人工接管阈值暂定为 10 分钟，每次重新提交的初审独立计时。
- AI 明确执行失败时可立即人工接管，无需等待 10 分钟。
- AI 仍在运行但达到 10 分钟时，审核专员可以接管；超时不等于执行失败，界面必须区分运行中、执行失败和超时可接管。
- 接管不是自动关闭：审核专员必须注明原因，随后直接人工审核，决定通过关闭或不通过退回。
- 人工接管后，迟到的 AI 结果仅留痕供参考，不覆盖人工裁决、不再次推进业务流程。
- AI 正常产出不通过意见时，仍必须经审核专员裁决，不自动退回。部分检查缺失如何归类及技术重试策略留待设计，不能伪造检查通过。

## 29. Agent 初审规则（v1.2 已确认）

### 29.1 检查项目

| 检查项 | 要求 |
|---|---|
| 整改前后图片 | Agent 使用视觉大模型对比整改前后图片，判断整改效果 |
| 短长期措施 | 对同一问题单的短期措施与长期措施进行雷同性审核，不扩展为历史跨单比对 |
| 文本有效性 | 检查原因、短期措施和长期措施中是否存在无意义、敷衍文字；不使用早期表述“无异议”作为规则 |
| 文本长度 | 原因、短期措施、长期措施分别不得少于 15 字，不能三字段合并凑字数 |
| 标点比例 | 每个字段独立计算，标点出现次数不得超过该字段全文长度的 10% |

初审意见交审核专员决定。字数、比例或语义检查发现问题均不得由 Agent 自动代替审核专员退回或关闭；是否增加提交前提示可作为交互设计，但不能把提示擅自变为自动退回规则。

### 29.2 已确认计数口径

对原因、短期措施、长期措施分别计算：

1. 换行不计数；排除换行后的全文长度记为 `L`，空格和标点均计入全文长度，不再排除空格。
2. 中英文标点均计数，空格也作为标点计数，出现次数记为 `P`，不是标点种类数。
3. `……` 作为一个标点识别计入 `P`，不因自身重复视为连续标点；其在全文长度中的字符计量应在技术设计和测试样例中明确，不能擅自将所有重复标点折叠。
4. 长度条件为 `L ≥ 15`。
5. 比例条件为 `P / L ≤ 10%`；等于 10% 满足条件，不得通过四舍五入放宽。
6. 不允许出现连续标点，`， 。` 也算连续标点，不能靠插入空格绕过；合法的单个 `……` 按上条例外识别。
7. 空文本不满足长度条件；实现必须避免除以零。
8. 实现可用 `10 × P ≤ L` 进行精确比例比较，避免浮点及展示舍入影响结果。

示例：`L=15、P=1` 满足长度和比例条件；`L=15、P=2` 不满足比例条件；`L=20、P=2` 满足；`L=14、P=0` 不满足长度条件。这些示例只验证格式规则，仍须进行语义审核。

建议字数与标点比例使用确定性代码计算并记录实际值，由 Agent 负责语义、措施雷同性及视觉分析。中英文标点集合、Unicode 字符计数及特殊空白处理需在技术设计中统一，不能由模型自由估算。

### 29.3 初审意见建议结构

建议保存各检查项状态、判断理由、问题片段或图片引用、文本长度、标点次数和比例、模型执行状态及关联提交版本。提供总览但不以单一总分覆盖具体失败项；审核专员能够查看原内容与逐项意见。

## 30. 巡检事项权限与审核员配置（v1.2 已确认）

### 30.1 移动端新建问题

- 增加巡检事项选择框。
- 按人员对事项的权限展示可选事项。
- 权限配置的默认语义以事项为中心：**某事项未配置权限，表示该事项无需权限即可选择**。
- 配置权限的事项，仅向拥有相应权限的人员展示为可选。
- 不能解释成“某人员未配置权限，因此可以选择全部受限事项”。

### 30.2 管理端维护

增加巡检事项权限维护，以及“巡检事项 × 厂区 → 审核员”配置维护。

- 每个事项与厂区组合固定配置一位审核员，不允许备用审核员，由管理员维护；不采用多人会审或随机选人。
- 缺少对应审核员时，保留提交并进入待配置状态，提醒管理员配置；配置完成后继续流转，不要求用户重复提交。
- 不跳过审核，也不自动指派其他人员。
- 管理员更换审核员后，未完成审核单转给新审核员，原审核员失去处理权限，并保留变更记录；已完成审核记录不改写。
- 事项是否必选、厂区字段与现有工厂数据的映射、管理员提醒渠道仍待细化。
- 不以隐藏基础信息区域为由移除审核员路由需要的厂区数据；路由数据与页面展示是不同问题。

## 31. 移动端 CPS 巡检页面精简（v1.2 已确认）

| 区域或字段 | 展示调整 |
|---|---|
| 基础信息区域 | 仅保留稽查人姓名、稽查时间 |
| 复核照片卡片 | 不显示 |
| AI 分类 | 不显示 |
| 人工分类 | 不显示 |
| AI 原因、AI 措施 | 合并展示为“AI 整改建议” |
| 人工原因、人工措施 | 原页面不再显示 |
| 底部节点卡片 | 不显示 |

### 31.1 展示与数据边界

1. 上述调整仅为页面展示精简，不删除后台字段、已有数据、历史记录、照片证据或工作流。
2. 原人工原因、人工措施展示区隐藏，不等于取消整改人员填写原因、短期措施及长期措施的表单；新办理表单仍按第 28 节保留。
3. 移除复核照片卡片不等于删除整改照片上传入口，也不能使 Agent 或审核专员无法获取需要的前后图片。
4. 基础信息仅显示两项，不自动取消新建问题时仍需填写或关联的业务信息；新增巡检事项选择框照常保留。
5. AI 原因、AI 措施合并为 AI 整改建议，是展示整合，不默认授权模型重新生成或覆盖历史内容。
6. 不因去除底部节点卡片而删除后台流转记录。

## 32. v1.2 验收清单与新增待确认项

### 32.1 验收清单

| 编号 | 验收标准 |
|---|---|
| AC-14 | 稽查人提交问题后进入整改处理，整改人员可填写原因、短期措施、长期措施、选择责任员工和上传整改照片，并可暂存与转办 |
| AC-15 | 选择责任员工不生成独立操作节点或独立流转待办 |
| AC-16 | 整改提交后产生包含前后图片对比、同单措施互比、无意义敷衍文字和字数标点检查的初审意见 |
| AC-17 | 三个文本字段分别执行第 29.2 节规则；长度 14 与 15、标点比例恰好 10% 与超过 10% 的边界结果正确 |
| AC-18 | AI 初审发现问题仍交审核专员处理，不自动退回；审核专员通过后直接关闭，不通过退回整改人员 |
| AC-19 | 新建问题显示巡检事项选择框：未配置权限的事项可直接选择；配置权限的事项按人员权限显示，不能把人员未配置当成全量开放 |
| AC-20 | 管理端可维护事项权限及事项与厂区对应的一位审核员；缺少对应审核员配置时提醒管理员，不跳过审核或任意派审 |
| AC-21 | 基础信息仅显示稽查人姓名、稽查时间；复核照片、AI分类、人工分类、原人工原因措施展示区和底部节点卡片不显示；AI原因措施合并为AI整改建议 |
| AC-22 | 页面隐藏后历史数据和照片证据仍保留，整改办理表单及上传入口仍可用，不影响 AI 初审与人工审核所需数据 |
| AC-23 | 本次 CPS 流程调整不解除辅房点检提交后的锁定，不恢复辅房语音判定或绕过照片要求 |

### 32.2 新增待确认项

- 暂存完整性限制、转办目标人员范围及是否跨厂区、退回意见要求。可转办阶段、仅当前承办人办理、不联动责任员工、退回重提必审已确认。
- AI 技术重试、部分意见缺失的分类及监控告警；失败立即接管、运行满 10 分钟可接管、必须说明原因及迟到结果不覆盖已确认。
- 巡检事项是否必选、厂区数据映射、管理员提醒渠道。缺审核员保留提交进入待配置、改配转移未完成审核单、禁止备用已确认。
- 特殊空白及 Unicode 字符计量实现细节、`……` 在长度分母中的计量，以及语义审核业务样例与阈值；中英文标点、空格算标点、换行不计数、连续标点和省略号例外已确认。
- 旧系统兼容性测试的数据范围和接口清单需技术核验；旧单按旧流程办结、新单走新流程已确认，不实施未经确认的旧单强制迁移。

## 33. v1.3 交付基线与补充验收（已确认）

### 33.1 范围和兼容边界

- 本 PRD 全部已确认功能一起交付，包括 CPS 新整改流程、辅房点检和排名、语音录入、周报与 Skill、巡检计划、台账出入库和库存预警，以及原有已确认能力。
- 第 14 节历史 MVP 分期仅作历史规划，不再作为将已确认功能延期或排除的依据；文档标为建议、待细化的方案不因此自动升格为已批准需求。
- 必须兼容旧系统：旧单继续原流程直至办结，新单采用新流程，历史数据及办理记录保留。不默认将存量未完成单迁移或重解释为新状态。
- 具体推送渠道为唯一在此明确的用户接入边界：提供周报推送代码接口，由用户实现渠道；不能以未实现真实渠道为由不交付报告生成、上传、运行记录或下载，也不能伪造发送成功。
- 本次仅同步需求文档，不代表已完成实现、联调或授权部署。

### 33.2 补充验收

| 编号 | 验收标准 |
|---|---|
| AC-24 | 三字段分别检测长度和比例；空格同时计入全文长度与标点次数，换行不计数，中英文标点均计数，`， 。` 判连续标点，`……` 按一个标点识别且不因自身重复违规；意见仍由审核专员裁决 |
| AC-25 | 缺审核员时提交内容保留并进入待配置，配置后继续流转；不产生备用审核员；管理员改配后未完成审核单交新审核员，原审核员不能继续审核 |
| AC-26 | 仅整改办理中和退回整改时允许编辑转办；提交锁定版本；转办后当前承办人办理并接收退回，责任员工不自动变化；退回重提重新触发 AI |
| AC-27 | 明确失败时立即开放人工接管；仍在运行未满 10 分钟不因耗时开放接管，满 10 分钟后开放；计时起点为该次初审提交成功；接管必须记录原因，迟到结果不覆盖人工决定或重复流转 |
| AC-28 | 周报东八区每周一 08:00 执行；统计窗口为上周一 00:00 至本周一 00:00、不含结束时刻；分类报告无需人工审核即可生成上传归档，记录可查且文件可下载 |
| AC-29 | 周报 Agent 使用报告 Skill；推送适配入口可接入，未接入显示“推送未配置”而非成功；具体渠道实现由用户负责，不要求本次交付真实发送渠道 |
| AC-30 | 计划审核时确定负责人和执行时间；批准后立即生成三类业务任务，按计划时间执行；部分创建失败后仅补建失败项，已成功项不重复创建 |
| AC-31 | 单次点检从 100 扣分、不适用不扣分、最低 0 分；同一自然周房间 A 两次得分 80、90，总分 170，房间 B 一次 100，总分 100，A 排在 B 前，不按均分或最新分排序 |
| AC-32 | 辅房报告展示每次检查时间、单次得分和扣分明细，以及检查次数、自然周总分和排名；分数与时间分字段但关联同一次记录，未提交及无效记录不作为已完成满分计入 |
| AC-33 | 超可用库存出库被拒绝且不生成负库存；库存小于等于阈值通知房间责任人，持续不足合并事件，大于阈值自动解除，再次不足形成新事件 |
| AC-34 | 旧流程未完成单能继续原流程办结，新建单按新流程处理；历史数据保留，不能因新页面隐藏而破坏旧系统办理及证据访问 |

### 33.3 验收说明

上述标准补充并细化 AC-01—AC-23；如历史验收要求周报必须人工审核或必须接通实际推送渠道，以本版为准。模型效果仍需业务确认样本，不能仅以接口返回成功代替视觉、语义和语音效果验收。剩余设计细节见更新后的第 27、32.2 节，不重复提问已确认规则。


