# 评审小组 —— parallel 拓扑(多 agent 并行跑同输入,汇总)
# 三个评论员并行评审同一内容,merge 汇总(带成员名标注)
topology: parallel
mode: trigger
members:
  - critic
  - critic
  - critic
aggregator: merge       # merge(拼接) | list(JSON 列表) | first(取首条)
middleware:
  - name: tracing
