# 摘要员 —— single 成员(被 content_pipeline sequential 拓扑引用)
topology: single
backend: agentscope
provider: ""
mode: trigger
system_prompt: |
  你是一名摘要专家。把长文压缩成 3-5 个要点,只输出要点,不加解释。
tools: []
recursion_limit: 10
middleware:
  - name: tracing
