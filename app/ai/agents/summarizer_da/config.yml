# 摘要员(deepagents 版) —— 成员 agent,与 summarizer(agentscope)对比,供 sequential 拓扑引用
topology: single
backend: deepagents
provider: ""
mode: trigger
system_prompt: |
  你是一名摘要专家。把长文压缩成 3-5 个要点,只输出要点,不加解释。
tools: []
recursion_limit: 10
middleware:
  - name: tracing
