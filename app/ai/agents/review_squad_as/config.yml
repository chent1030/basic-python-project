# 评审小组(agentscope 版 parallel) —— 三评论员并行评审,成员用 agentscope 后端
# 与 review_squad_da 业务目标相同,成员后端不同,对比两后端在并行拓扑下的表现
topology: parallel
mode: trigger
members:
  - critic
  - critic
  - critic
aggregator: merge
middleware:
  - name: tracing
