# CPS 开发进展日志

> 跟踪开发计划（docs/CPS智能巡检系统开发计划.md）执行进度。每波次一节：范围、交付、证据、遗留。

## M0 — Phase 0 前置核验 + 基线冻结【已完成】

- 双侧核验报告：docs/Phase0核验/{前置核验报告-Java侧,前置核验报告-Python侧,前置核验报告-汇总}.md
- 关键结论：A1/A5 可立即启动；C-01~C-09 双侧全绿地；计划主体成立无需重排。
- 新增风险：R-N1 附件断点（P0，线上无图巡检）／R-N2 明文 API key／R-N3 MySQL 启动方式待验证／R-N4 七对象全 SQLite 原型（线 I 绿地）／R-N5 vector-service 零调用。
- 用户决策（3 项）：① M0 基线提交 ✓（cps `68f3511`，basic-project `78f7209`）；② D-22 = 字符数 code point 口径；③ API key 仅代码改 env 优先、暂不轮换。
- 遗留：V999999 seed 脚本损坏待修复；empName 工号占位 + DEV_EMP 无鉴权为技术债；T0.3 异常注入待联调环境。

## 波次 1 — 线 A1(Java) + 线 A5(Python) + R-N1 修复【已完成，待提交后回填 hash】

### Java 侧交付（cps/backend，42 文件）

- **附件断点修复（R-N1）**：`service/CpsAttachmentContentResolver.java`（content 空且 object_key 存在→RustFS 流读取，复用 `RustFsStorageService.read()`/MinIO SDK 零新增依赖；失败快速抛错禁止静默降级）；`CpsAgentFrameworkClient` 消除静默跳过 + 新增 C-01 `triggerInitialReview`/C-03。
- **数据底座**：`db/migration/V20260923__cps_rectification_flow_v2.sql` = cps_issue +5 列（flow_version 默认 'legacy'、short/long_term_measure、current_submission_version 等）+ 4 新表（cps_rectification_submission/cps_initial_review_task[六态]/cps_initial_review_result/item/cps_rectification_transfer）+ 4 唯一索引。**已在真实 cps 库应用并启动验证**。
- **V2 状态机**：`CpsWorkflowStateMachineV2`（提交落点：审核员可解析→PENDING_AI_REVIEW，缺配置→PENDING_REVIEWER_CONFIG）；旧 `CpsWorkflowStateMachine` 冻结；`CpsIssueService.executeAction` 按 flow_version 路由，新单写 'v2'。
- **契约与服务**：`CpsInitialReviewService`（535 行：幂等建任务/事务后投递/回调幂等+迟到留痕/30s 超时扫描/takeOver 拒绝 RUNNING/审核员解析）；`CpsInitialReviewCallbackController`（C-02，不可信 403）；`CpsCallbackTrustService`（只信 remoteAddr，默认 127.0.0.1/::1 CIDR 可配）；调度器 + `@EnableScheduling`。
- **测试**：`mvn test` 82/0/0（新增 33：状态机 6、resolver 4、客户端+P0 回归 6、初审服务 17、信任 4）；JDBC 冒烟 + 真实库迁移 + `java -jar` 启动 + C-02 冒烟。

### Python 侧交付（basic-project，12 项）

- **A5 文本规则引擎**：`app/projects/initial_review/domain/text_rules.py`（D-22 口径常量：code point、换行集 {\n,\r,U+2028,U+2029}、L≥15、10×P≤L 整数比较、空格计 L 与 P；`……` 游程=(k+1)//2 计 P、游程内不判连续、`,……`判连续）+ `check_text_rules`/`check_rectification_texts` 三字段独立纯函数。
- **契约骨架**：`app/api/v1/endpoints/agent_callbacks.py`（C-01 POST 202+review_task_ref 幂等 / C-03 GET）；`app/projects/initial_review/`（service：8min wall-clock deadline、乐观锁终态转移、惰性超时回收；infrastructure：repository/callback[30s/60s 退避]/config）；未实现检查显式 SKIPPED，overall=PASS/PARTIAL/PROBLEM 不伪造通过。
- **PG 表**：alembic `b41c7f2ae903_create_agent_initial_review_exec`（down_revision=9d35bb4a1977），本机 PG 实际 upgrade + downgrade 往返验证；ORM `app/models/agent_initial_review.py`。
- **API key**：`config/agents.yaml` qwen `api_key_env: DASHSCOPE_API_KEY`（现值保留待用户轮换），读取优先级 env>yaml。
- **测试**：新增 46 全绿（text_rules 29 + service 11 + router 6，0.35s）；全量 288 passed/2 failed/1 skipped，2 个失败经 `git stash -u` 基线对照确认**为 M0 基线既有**（local.yaml auth.enabled=false 所致，非回归）；ruff 全过。

### 双侧对齐点（联调依据）

- 幂等键：任务 `cps-rectify-{issueId}-v{n}`；结果回写 `initial-review-result-{taskId}`。
- C-01 载荷 snake_case（submission_id/version_no/short_term_measure/before_images[].content_base64/callback{url,task_id,idempotency_key}）；响应 `review_task_ref`||`id`。
- C-02：`task_id`/`idempotency_key` 任一定位；`error_code` 非空=失败；逐项含 text_length/punctuation_count/ratio_ok；**is_late 由 Python 恒发 false、Java 按自身任务状态判定**。
- 路径：Java baseUrl 已含 `/api/v1` → 实际端点 `/api/v1/agent/rectifications`（联调时核对）。
- 回调地址：yaml `cps_agent.java_callback.base_url`（默认 http://127.0.0.1:8080），env `CPS_JAVA_CALLBACK_BASE_URL` 优先。

### 待联调清单（波次 2 前置）

1. C-01/C-02 端到端调通（双侧均已就绪，建议波次 2 首先做 J0 联调）；2. 附件双模式（object_key 流转发 × base64）；3. 回调重试退避 × Java 幂等去重；4. 鉴权口径确认（identity.require("cps_admin") 内网信任）；5. 迁移在目标环境执行。

## 波次 2 候选（按计划关键路径）

1. **J0 提前联调**：C-01→执行→C-02 全链路真实打通（不等 J1）。
2. 线 A6/A7/A8：初审视觉/语义组件（依赖 dashscope key 有效，模型调用真实化）。
3. 线 B1/B2（辅房配置底座）+ E1（台账）并行：Java 侧。
4. D-16 样本集：提醒业务方（W10 前确认，硬前提）。

## J0 提前联调（2026-09-22，波次 2 首项）

**结果：C-01 → Python 执行 → C-02 回调 → Java 终态 全链路 PASS**（脚本 `scripts/j0_initial_review_e2e.py` 可重跑，凭据走环境变量）。

### 暴露并修复的真实缺陷（联调价值证明）

1. **[Python] agent_callbacks.py 装配路径 bug**：`request.app.state.datasources` KeyError——`DatasourceManager` 实为模块级单例（`app/core/datasource.py`，main.py 同源导入），不在 app.state。单测经依赖注入未暴露真实装配路径。修复：改 `from app.core.datasource import datasources as shared_datasources`。→ 波次 2 补真实装配回归测试。
2. **[Java] C-02 DTO 类型契约 bug**：`CpsInitialReviewCallbackRequest.task_id` 声明 `Long`，但 C-01 载荷不含 Java 数字主键、Python 只持有字符串引用 `cps-rectify-{issueId}-v{n}` → Jackson InvalidFormatException 整包 400。修复：task_id 改 String；`resolveTask` 三级定位（数字串→findById；cps-rectify-*→findByIdempotencyKey；initial-review-result-{…} 前缀解析；issue_id+version_no 兜底）。测试适配 String.valueOf。`mvn test` 全量通过后重打包重启。
3. **[配置] PG 连接占位符**：local.yaml:29 `enc:REPLACE_WITH_ENCRYPTED_PASSWORD` → 5432 实为容器 ai-experience-postgres（密码 Ct0520.0402，库 app，alembic 表已在其内）。已改明文（该文件自述允许本地明文开发；gitignored）。启动日志 "datasources ready" 为懒连接假象——真实连接错误延迟到首次查询。

### 契约澄清（写入联调结论）

- C-01 载荷 `issue_id`/`submission_id` 为 **字符串**（Python schema string_type）；`review_task_ref == task_id == 幂等键`。
- **重放语义**：同载荷重放 → 202 + `replayed:true`（返回缓存结果，**不重发回调**）；同键异载荷 → 409。Java 侧恢复路径=30s 扫描器（timeout_at=submitted+600s → TIMEOUT_OPEN），符合 PRD §28.4 迟到留痕。
- 回调失败在 Python 侧落库 `callback_status=FAILED`（exec 表）——波次 2 改进候选：重放请求且 callback_status=FAILED 时补发一次回调（Java 幂等可去重）。
- 无附件路径：IMAGE_COMPARE/MEASURE_SIMILARITY/TEXT_VALIDITY=SKIPPED（不伪造通过），overall=PARTIAL 由文本规则驱动（model_version=text-rules/d-22@1）；语义/视觉检查待线 A6/A7 接入。
- 断言通过样本：reason L=32 P=3（10×3≤32 ✓）、short_term/long_term L=26 P=2 ✓。

### 环境事实（后续联调沿用）

- cps-agent/basic-project/.venv 为拷贝目录，bin shebang 指向 /Users/csai/project/basic-project/.venv（解释器/依赖实际来自原项目，可用但非独立）。
- Java 重启需强杀：`pkill -9 -f cps-backend-0.0.1-SNAPSHOT.jar` 后再 `java -jar`（普通 pkill 有端口竞争）；空体探活 /api/callbacks/initial-review/result → 400 为正常。
- 遗留：postgres_readonly/mysql_business 仍为占位符（懒加载不阻塞）；Python 启动初期瞬态 500。

### 提交

- cps：DTO/Service/Test 三文件（task_id String + resolveTask 三级定位）。
- basic-project：agent_callbacks.py 单例修复 + scripts/j0_initial_review_e2e.py + 本日志。

## 2026-09-26 波次2 Java 侧：A2 事项锁定/审核改配/乐观锁 + B1/B2 辅房配置 + E1 设备台账

### 提交

- cps（详见 docs/波次2-Java侧-A2B1B2E1设计.md）：A2 七文件（Controller/Issue/Action/Mapper/Service/StateMachineV2 + CpsReviewerReassignRequest）+ V20260924 乐观锁迁移；B1/B2 五表 V20260925（cps_room/cps_check_item/cps_inspection_item/cps_inspection_item_permission）；E1 V20260926（cps_inventory_item，预警语义 stock_qty<=alert_threshold 含等于）+ domain/dto/mapper/service/controller 全套。
- 测试：mvn test 115 全绿（82→95→115；A2 +13、B1/B2/E1 +20）。
- 真实库：Flyway 7.7.3 实际执行 V20260924/25/26（history success=1×3），冒烟 INSERT 事务内回滚无残留。

### 契约口径

- §30.1 查看权限：无配置行=开放，有配置行=仅授权工号（PUT permissions 整体替换，空名单=回到开放）。
- §30.2 审核员改配：POST /api/cps/issues/{id}/reassign-reviewer；三审核态可改配，PENDING_REVIEWER_CONFIG 单续路 PENDING_REVIEW，PENDING_REVIEW 同步切 currentHandler；AI 初审中改配不触碰初审任务。
- §28.3 锁定：PENDING_AI_REVIEW/PENDING_REVIEWER_CONFIG/PENDING_REVIEW 三态禁编辑/转办；仅 PENDING_RECTIFY 可编辑。
- AC-24 乐观锁：cps_issue.lock_version CAS（updateWorkflowFields 返回行数，!=1 抛并发冲突）。

### 【待确认】

- "点检项目配置表"两读覆盖（cps_check_item 辅房点检项 + cps_inspection_item 巡检事项）；辅房适用范围先 applicable_room_types CSV（空=全部）；§23.1 base/factory 列预留 NULL 待口径。

## 2026-09-26 波次2 Python/Agent 侧：A6 雷同性 + A7 视觉 + A8 文本有效性 + A9 汇总 + 装配回归

### 提交

- basic-project（详见 docs/波次2-Python侧-A6A7A8A9设计.md）：
  - 新增 `application/check_pipeline.py`（四项检查编排+历史合并）、`domain/{verdicts,measure_similarity,text_validity,image_compare}.py`、`infrastructure/{model_client,prompts}.py`；
  - service.py 接线 CheckPipeline（CheckRunner 签名扩展 request|None，重放路径 base64 附件如实 DEGRADED）；callback.py 增 overall_note；config.yaml 增 similarity/model_check/rustfs 段；
  - 降级三态 IMPLEMENTED/DEGRADED/SKIPPED：技术失败不冒充内容判定，模型禁用经 env `CPS_INITIAL_REVIEW_MODEL_CHECK_ENABLED`；
  - 测试：pipeline 18 + similarity 11 + verdicts 5 + assembly 3（真实装配冒烟，PG 不可达 skip）+ scripts/wave2_real_model_smoke.py（手动真实模型冒烟）。
- 测试：pytest 350 通过/2 既有环境失败（与基线一致无新增）/1 skipped；本波次 +62。ruff 全绿（顺手修 agent_runs.py/j0 脚本存量 lint）。

### 契约口径

- A6：bigram Jaccard；同单 short↔long ≥0.90 FAIL/≥0.75 WARN；vs 历史 ≥0.85/0.70（D-05 暂定阈值，config 可配）。
- A7：一次视觉调用完成前后比对+读数核对；读数不一致强制 ≥WARN；附件 object_key(RustFS)/base64 双模式。
- A9：任一 FAIL→PROBLEM；有 WARN/SKIPPED→PARTIAL+明细；全 PASS→PASS；全 SKIPPED→PARTIAL。

### 【待确认】

- D-05 阈值校准（W3 前业务样本）；历史相似度为任务书扩展口径。

## 2026-09-22 波次3 Java 侧：C5 周报代理 + B7 数据源 + D1/D2/D3 计划建单

### 提交

- cps（详见 docs/波次3-Java侧-C5B7D1D2D3设计.md）：
  - **C5/C8**：`CpsAdminWeeklyReportController`（`/api/cps/admin/weekly-reports` 列表 + `/{runId}/download` 流转发）+ `CpsWeeklyReportService`（参数透传/响应归一化/窗口校验）+ `CpsAgentFrameworkClient` 扩展 `requestInspectionPlanDraft/listWeeklyReportRuns/downloadWeeklyReportFile`（C-07/C-05/C-08）；
  - **B7**：V20260927 五视图（weekly_report_data_issue_summary/rectification_summary/initial_review_summary/check_item_snapshot/inventory_low_stock）+ `CpsWeeklyReportDataMapper.xml` + `CpsWeeklyReportDataService`（窗口 Window [start,end) 强校验；`currentWeekWindow` 东八区上一完整自然周）+ `CpsWeeklyReportDataController`（5 端点 + window/current）；
  - **D1/D2/D3**：V20260928 `cps_inspection_plan` + `cps_inspection_plan_task`（UNIQUE(plan_id,task_type) 幂等 + lock_version CAS）+ `CpsInspectionPlanService`（applyDraft/approve/reject + 私有 createPlanTasks 三类补建）+ `CpsInspectionPlanController`（apply-draft/list/detail/approve/reject）。
- **测试**：mvn test 151 全绿（115→151；新增 36：plan 18 / weekly proxy 6 / weekly data 6 / agent client 6）。
- **真实库**：Flyway V20260927/V20260928 实际执行（history success=1×2）；冒烟：`POST /apply-draft` → 列表 → 批准（3 createdTasks 落库）→ 删 INSPECT_CHECK 再批准（自动补建 1 项，UNIQUE 兜底验证）。B7 `/weekly-report-data/window/current` 返回 2026-09-14~2026-09-21（东八区）。

### 契约口径

- §21.3 周报运行：Java 仅代理，PG 真实表在 Python；C-05/C-08 沿用 `cps.callback.trusted-ips` 思路（生产环境网关层做内网 CIDR 校验）。
- §21.1.6 窗口：左闭右开 `[period_start, period_end)`；东八区今日 2026-09-22 → 上周 `[2026-09-14 00:00, 2026-09-21 00:00)`。
- §22.2 / AC-30：D3 建单事务 `findExistingTaskTypes` → 仅 INSERT 缺失 task_type → UNIQUE 兜底；同 plan 重启/重试批准仅产 3 任务；comment 落 `acceptance_criteria` 默认值。
- 任务类型枚举 `INSPECT_RECTIFY/INSPECT_PATROL/INSPECT_CHECK` 与 §22.1"整改复查/区域巡检/辅房点检"映射。

### 【待确认】

- 任务类型枚举名是否需改为中文/业务命名；§21.1.8 推送状态 `UNCONFIGURED` 本期仅透传不阻断下载（与 §33.1 用户接入边界对齐）；C-07 Python 端实现可后置（Java 已就位）。


## 2026-09-26 波次3 Python/Agent 侧（补完）：C1 Skill + C2 Agent + C3 调度 + C4 归档 + C6 推送 + C-05/C-08 端点 + C-07 计划草稿

### 提交

- basic-project（详见 docs/波次3-Python侧-C1C2C3C4C6C07设计.md）：
  - **C1 Skill**：`app/skills/weekly_report/{skill.py, templates.py}`；Protocol `render(spec, data)→RenderedReport`；默认模板拼装（无 LLM，D1 Agent 后可替换）。
  - **C2 报告 Agent**：`app/projects/weekly_report/application/service.py` 编排；advisory lock(0x57524550='WREP') + UNIQUE 兜底同窗口幂等；状态机 PENDING→RUNNING→ARCHIVING→COMPLETED/FAILED。
  - **C3 调度**：`app/tasks/weekly_report_task.py` APScheduler cron `0 8 * * MON` Asia/Shanghai，env `CPS_WEEKLY_REPORT_SCHED_ENABLED` 默认 false 防开发机误触发。
  - **C4 归档**：`alembic c3a1b2d3e4f5_create_weekly_report_run`（down_revision=b41c7f2ae903，24 列 / 6 索引 / 5 态 status + 5 态 push_status 解耦）；已应用于 ai-experience-postgres。
  - **C6 推送**：`Pusher` Protocol + `NoOpPusher`(`UNCONFIGURED` + "推送未配置")，AC-29。
  - **C-05/C-08**：`/api/v1/agent/weekly-reports` 列表 + `/download` 受控流式下载；复用 `Identity.require('cps_admin')`。
  - **C-07（本轮补完）**：`app/projects/inspection_plans/application/service.py` + `app/api/v1/endpoints/inspection_plans.py`；任务蓝图按 `plan_type` 决定优先级权重（WEEKLY_RECTIFY→RECTIFY 高/...）；进程内 LRU+TTL 幂等（重放 status=REPLAYED）；路由注入 `app/api/v1/router.py`。
- **测试**：pytest 395 通过/2 既有/1 skipped（+15 本轮：C-07 service 8 + endpoint 7）；ruff 全绿。
- **真实环境**：PG alembic 已落；`scripts/c3_weekly_report_smoke.py` 双分类全 PASS；RustFS 容器 healthy（9000/9001）。
- **配套 Java**：CpsAgentFrameworkClient.requestInspectionPlanDraft 已就位（波次 3 Java），本期 Python C-07 对接闭环。

### 【待确认】

- D-10 周报重跑版本化（当前同窗口跳过 vs 新 run_no）；
- D1 计划 Agent LLM 实现（确定性模板 + Protocol 扩展点已留）；
- 任务枚举名 `INSPECT_*` 是否需改业务命名（与 Java D3 一致）。

## 2026-09-26 决策会（用户确认 11 项）→ docs/决策记录-20260926.md

- **D-01 关闭：不兼容旧数据**（C-10 既有链路/AC-34 旧单回归取消；flow_version 不再新增兼容逻辑）
- **D-02 关闭：周报就两类**（cps-issue-inspection / room-check）
- **D-04 关闭：事项必选 + 厂区走主数据表 + 提醒渠道用户自写（TODO-1）**
- **D-03 关闭：语音覆盖 原因/短期措施/长期措施 三字段**
- **D-05 关闭：阈值可配置+纯中文注释**（本次提交同步去除周号代号）
- **D-08 关闭：同分按完成时间，不跨周**（B4 实现口径）
- **D-10 关闭：周报重跑同窗口跳过**（现行为即此）
- **D-13/权限设计取消：默认全员 Agent 权限** → H 线缩减（H1/H2/H4 取消；permission 表保留"无配置行=开放"行为）
- 待确认剩 3 项（附白话解释在决策记录文档）：D-22 口径常量表、D-16 业务样本集、D-17 记忆审核角色（建议取消）

## 2026-09-26 决策第二轮（剩余 3 项关闭）

- **D-22 口径常量表：确认冻结**（空格算标点/`……`=1/换行不计/相邻标点不过，代码+单测已固化）
- **D-16 样本集：走 API**——Agent 从接口拉取历史数据，不做人工导出；评估脚本调 Java 数据接口，不足则补只读端点（TODO-2 转开发项）
- **D-17 记忆审核角色：取消**；不做记忆人工审核
- **主计划 D-01~D-22 需用户拍板项全部关闭**，后续按决策记录文档推进

## 2026-09-26 TODO-3 完成：主计划头加决策更新横幅（旧单兼容作废/权限取消/决策全关），冲突处以决策记录为准

## 2026-09-26 波次 4：B3 点检流程 + B4 评分排名（Java）+ B6 视觉点检 Agent + C-04（Python）

- **Java cps `39ea9f0`**（基线 7aa87a0）：B3 点检执行域（cps_room_check_record(+_item)，V20260929，明细=点检项全快照；mobile 任务/明细/照片/提交端点；提交同步调 C-04，Python 未起降级 PENDING 不阻塞；JUDGED 后锁定）+ B4 评分排名（100−FAIL 扣分、自然周 SUM、同分最早提交升序=D-08 口径、admin 排名/明细端点）。mvn **176/0**。真实环境冒烟通过（Flyway+start→双照→降级提交→锁定 400→排名聚合）。
- **Python basic-project `5d48c19`**（基线 962ac91）：B6+C-04 `POST /api/v1/agent/room-checks/judge` 同步判定（RustFS 取图→类型匹配 TYPE_MISMATCH 短路→内容判定 PASS/FAIL/UNJUDGEABLE→stage_trace+item_snapshot 证据；幂等键 room-judge-{submissionId}-{itemId}-{attempt}；enabled=false→SKIPPED 不伪造；每阶段 90s 预算）。pytest **414p/2f（既有）**+19 新测试，ruff 零新增违规。
- **J 线 C-04 联调清单**（双方待确认合并）：①Java HTTP 超时 ≥240s 或独立预算 ②响应字段 itemId/itemCode 对齐 ③502+error_code vs 200 三态口径 ④photoBase64 大图 payload 改 URL-only ⑤PENDING 单补判定解锁/重跑入口 ⑥模型开关与 initial_review 共用需独立 ⑦qwen-vl 真实链路冒烟 ⑧Java 默认 judge 端口 8010 vs Python 实际 8000，联调时对齐配置。

## 2026-09-26 波次 5：A3 审核裁决 + A4 初审触发管理（Java）+ F1/F2 语音 + C-06（Python）

- **Java cps `90cb0de`**（基线 39ea9f0）：A3 裁决域（cps_review_adjudication uk(issue_id,version) 幂等；APPROVE→REVIEW_CLOSE→CLOSED / REJECT→退回 PENDING_RECTIFY；ai_relation 矩阵 NO_AI_RESULT/WITH_AI/AGAINST_AI；三态接管 AC-27：running 未满 600s 不可接管、迟到结果仅留痕）+ A4 触发管理（cps_initial_review_config 单行 GLOBAL：autoTrigger/max_retry/backoff；cps_initial_review_event 12 类事件流水；重触发新幂等键 -r{n} 独立计时）。mvn **212/0**。mobile 三态视图/接管/裁决 + admin config/tasks/retrigger 端点。
- **Python basic-project `6f594e0`**（基线 23f04af）：F1+F2+C-06 speech-to-text 同步端点（app/projects/speech/，三字段白名单 reason/short_term/long_term=D-03，未配置 SKIPPED 不伪造，幂等键惯例）。pytest **449p/2f/1s**。4 待确认项见设计文档 §8。
- 教训（专家反馈）：python heredoc 改枚举静默丢失，代码修改必须用 edit 工具。

## 2026-09-26 波次 6：B5 移动端页面 + F3/F4 语音集成（Java）+ A10 边界测试 + C7 契约冻结（Python）

- **Java cps `8ccb92c`**（基线 90cb0de）：新端点 POST /api/cps/speech/transcriptions（multipart→RustFS→同步调 C-06，timeout 240s 可配）；三新页面（点检执行全流程含 PENDING 降级态/初审三态+接管+裁决 WITH_AI·AGAINST_AI/VoiceInput 按住录音转写回填三字段）；§31 精简（隐藏不删除，?legacy=1 可达）；附带修复 vitest 基线崩溃（uni-cli-shared module-alias 劫持 vue，VITEST 分支 pin 真实路径）。mvn **230/0**；mobile pnpm build 过；vitest 32 绿/5 红（波次2-5 旧债）。
- **Python basic-project `85654c1`**（基线 5b67bfd）：+42 测试（A10 D-22 逐条边界+服务边界 ×20；C7 契约 schema 冻结 C-01/03/04/06/07 ×14；周报端到端装配 ×4；计划批准重放幂等 ×4）。pytest **491p/2f/1s**。
- **发现产品 BUG-1（中危，未修）**：initial_review `service.py:58` CheckRunnerError 死代码——RustFS TCP 级 httpx.ConnectError 裸抛穿透两层 except → HTTP 500 + 行卡 RUNNING 被扫描误标 TIMEOUT；设计 FAILED+CHECK_ERROR 路径不可达。观察项：run_once 无 run_no；replayed=True 是行存在语义。

## 2026-09-26 BUG-1 修复（主会话直接修，非波次）

- fetch_bytes 捕获 `httpx.TransportError` → 包成 `ModelCallError`（与 HTTP≠200 同语义 → 管线 DEGRADED 不伪造），保留异常链
- `_execute` 兜底 `except Exception` → FAILED+CHECK_ERROR+正常回调（意外异常不再 500/卡 RUNNING 被误标 TIMEOUT；原 CheckRunnerError 分支保留）
- 新增回归测试 ×3（ConnectError→DEGRADED / 包装单测 / runner 爆炸→FAILED+回调 CHECK_ERROR）；全量 494p/2f（既有）/1s，ruff 全过

## 2026-09-26 波次 7：J 线联调（Java 全链路驱动 + Python 独立开关与真实冒烟）

- **Java cps `71bcbee`**（基线 8ccb92c）：四条端到端链路真服务全跑通——①初审全闭环（issue7：建单→指派→整改→C-01 真实初审 17s PROBLEM→回调→接管三态→裁决→CLOSED；降级路径 issue6 投递失败→PENDING_REVIEW 接管同验）②点检（计划→批准→建单→拍照→submit→C-04 502 真实降级 PENDING 不阻塞→rejudge attempt=2→排名口径正确）③语音（TRANSCRIBED 空判正确+502→UNAVAILABLE）④周报（list/download 契约对齐+归档回源）。C-04 清单 Java 侧 ①③④⑤⑧ 全关（240s 超时/502 降级/object_key/rejudge 端点 V20261002 judge_attempt/端口 8000）。修 5 个 Java bug（建单 version 默认值/rectifications 路径 404/comment 截断/attachment_id 字符串化/初审超时 10s→180s）+ C-01 载荷按 C7 冻结 schema 重写。mvn **242/0**。报告：cps/docs/波次7-J线联调报告.md。
- **Python basic-project `99c8380`**（基线 b205c82）：独立模型开关（清单⑥）——三级回退「新 env > 新 yaml > 旧配置」，room_checks.vision.enabled / speech.asr.enabled 独立于 initial_review，全未配默认 false→SKIPPED（reason 含决策来源），向后兼容零变化。真实模型冒烟（清单⑦，真实 DashScope key）：C-04 TYPE_MISMATCH 真实 qwen-vl 判定成功、C-01 CHECKED 结构化输出成功、C-06 网关通（合成音空转写=预期）。**发现并修复：`vision_model: Qwen2.5-VL-7B-Instruct` 在 DashScope 404（波次4起潜伏，Fake 测试从未暴露）→ 改 `qwen-vl-plus`**；qwen-vl 图片边长需 >10px。run_no 补齐（4 处有行返回，无行不伪造）；replayed=True 语义写入注释与契约文档。pytest **506p/2f（既有）/1s**，ruff 过，schema 冻结未破。交付：docs/波次7-Python侧-开关与真实冒烟.md + scripts/j7_real_model_smoke.py。
- **交叉核对（Java 转交 6 项 → Python 侧处置）**：#1 模型 404 ✅已修（qwen-vl-plus）；#5 report_type 枚举 ✅Java 已适配 Python 枚举透传（链路4过滤通过）；#2 结构化输出解析失败 → 部分（冒烟成功但初审阶段仍有重试兜底场景，观察）；#6 probe 残留 → 知悉无影响；#3 重放不补回调（Java 靠 600s 超时兜底）、#4 回调重试耗尽悬挂（缺重推接口）→ **遗留待办**。
- **波次 7 遗留**：①重放补回调/回调重推接口（Python）②初审结构化输出鲁棒性（prompt JSON 约束/解析容错）③C-04 验到 JUDGED 需真实地面照片 ④ASR 真实音频准确率 ⑤agents.yaml:19 404 模型（框架线）。

## 波次 8（2026-09-23，E 线台账库存全链 + D 线收尾 + J 线遗留三件套 + I 线重估）

双专家派发双双 error（token 窗口耗尽）后由主会话亲自续完两侧半成品并补齐缺口。

**Java 侧（cps `061a445`，mvn 265/0，admin build 过）**
- E2：`cps_inventory_txn` 流水（V20261003），`selectForUpdate` 行锁串行化 + before/after 快照 + IN/OUT/ADJUST 符号 CHECK + 禁负库存；端点 POST/GET `/api/cps/admin/inventory-txns`（CpsPageResponse total+rows）。
- E3：`cps_inventory_alert_event` 合并事件（生成列 `open_item_id` + UNIQUE 兜底同物品同时最多一行 OPEN）；`stock_qty <= alert_threshold` 含等于（AC-13）；回升自动 RESOLVED_AUTO、人工 IGNORE/RESOLVED_MANUAL（关闭必填原因）；端点 GET `/inventory-alerts` + POST `/{id}/handle`。
- E4：admin 前端 `InventoryPage`（路由 `/inventory`「物品台账」）：台账列表（低库存徽标/启停/新增编辑）+ 出入库登记弹窗与流水分页 + 预警处理 Tab（忽略/关闭）。api.ts/types.ts 增补，PageHead.detail 放宽为 ReactNode。
- D4/D5：`cps_plan_build_record`（V20261004，append-only，CREATED/SKIPPED_EXISTING/CREATE_FAILED × APPROVE/REBUILD）；建单引擎单项失败不阻断其余类型；端点 `GET /inspection-plans/{id}/record-status` 聚合视图 + `POST /{id}/rebuild-tasks` 仅补建。D5 并入 CpsInspectionPlanServiceTest（41 个，+17）。
- 补 `CpsWave8MapperContractTest`（5 个：FOR UPDATE/快照列/生成列唯一/append-only/迁移约束）。
- 迁移 V20261003/V20261004 真实库已应用（Flyway 校验 20/20），三端点真实库冒烟通。
- 设计文档：`cps/docs/波次8-Java侧-E线D线收尾设计.md`（专家死前已落盘）。

**Python 侧（basic-project `252a9d3`，pytest 528p/2f[既有]/1s，ruff 绿）**
- 转交 #3 终态重放补回调：submit 命中提交时已终态行（`existing_status != "RUNNING"`）→ 补发一次回调（同幂等键，Java 去重）。门槛必须用提交前状态，否则重放 RUNNING 行会重复回调（半成品 bug 已修）。
- 转交 #4 手动重推：`POST /api/v1/agent/rectifications/{task_id}/callback/re-push`（cps_admin；RUNNING→409、配额 20→429）。
- 转交 #2 结构化输出鲁棒性：prompt v2（`_JSON_ONLY_RULE`）+ `extract_json_object` 多级降级（直解/栅栏/平衡括号/修复重试）。
- 文档：`docs/波次8-Python侧-J线遗留三件套.md`、`docs/I线缺口重估-20260926.md`。

**用户裁决修正（2026-09-27）**：I线重估建议方向错误——原建议"FR-09/11/12 整体关闭、FR-08/10 挂起"是对 D-17 的过度引申（D-17 取消的是记忆审核**人**角色，非记忆能力本身）。记忆体系是产品核心，agent 必须根据历史审核情况自主进化、使用越久越贴合实际。修正结论：FR-09/10/11/12 全部"必须补做"，FR-08 不挂起；FR-03/04 保留为框架能力不做业务编排，FR-06 不做编辑环节。文档已更新（同步追加自我反思：治理结构决策不应连带关闭被治理的能力层）。

## 波次 9（2026-09-27，I 线记忆体系骨架 + FR-09 长期记忆接入 + F 线推送配置）

用户裁决修正后立即推进——记忆体系是产品核心，agent 必须根据历史审核情况自主进化。

**Java 侧（cps `68d77a4`，mvn 272/0，+7 Wave9 契约测试）**
- I 线消费端：`/api/cps/admin/memory/{adjudications,events,issues}` 三分页查询端点，复用 cps_issue JOIN 4 路分类 + 全部过滤谓词。smoke 真实库：adjudications=0 / events=20 / issues=7。
- F 线推送配置（D-22）：V20261005 `cps_push_config` 单行 GLOBAL（CHECK id=1 + 生成列 enabled_channel + UNIQUE 启用去重），channel 枚举 INTERFACE/EMAIL/SMS/WEBHOOK，secret_ref 仅 KMS 引用。GET/PUT 端点 + 校验逻辑（enable=true 但 endpoint 空 → 400）。
- 真实库校验全过：Flyway 校验 cps schema up to date，4 端点 smoke 全通。

**Python 侧（basic-project `29c3ba4`，pytest 568p/2f[既有]/1s，+40 记忆套件全过，ruff 绿）**
- 骨架：`app/skill/memory/{domain,infrastructure,application,api}/` 4 层 + Ingestion/Retrieval/Clustering 三服务。
- 表：`memory_entry`（UNIQUE source_table+source_id，embedding vector(1024)，soft-supersede）/ `memory_skill_pattern` / `memory_metric_snapshot`。
- 迁移：`V20260927__memory_schema.py`（含 ivfflat cosine 索引）。
- 客户端：`CpsMemoryClient` 调 Java 三端点，分页+重试+指数退避。
- Embedding：`HashPlaceholderEmbedding`（SHA256→1024 维归一化，TODO 真实 BGE-large 接入）。
- AI 初审接入：`_execute` 路径调 `retrieve_context_for_issue(issue_dto, top_k=5)` → 拼 hints → 写入 `snapshot["historical_hints_json"]`，try/except 兜底不阻断主流程。
- Prompt 升 v3（`text-validity/qwen-plus@3` / `image-compare/qwen-vl@3`）+ `HISTORICAL_HINT_SECTION`。
- 文档：`docs/波次9-Python侧-I线记忆体系骨架与FR-09.md`。

**待办（后续波次）**：真实 BGE embedding + procedural 层（FR-11）/ 效果评估（FR-12）/ 覆盖分析（FR-10）/ 自动聚类 scheduler / 跨库迁到独立 `cps_memory` / `memory_retrieved` 写 `AgentInitialReviewExec` audit log。

## 波次 10（2026-09-27，FR-10 历史/覆盖分析）

**Java 侧（cps `1cdeb66`，mvn 277/0，+5 契约测试）**
- `/api/cps/admin/coverage/{frequency,region-supervisor,recurrence,gaps}` 四聚合端点
- 真实库 smoke：frequency=4 行、region-supervisor=4 行（DEMO_RECTIFY 2 在手/2 超期）、recurrence=空、gaps=空
- 关键契约：frequency `GROUP BY factory, area, category_l1_id` + 30 天窗口；region-supervisor `overdueDays` 参数化 + `__OPEN__` 哨兵映射 `<> 'CLOSED'`；recurrence `UNION ALL` flow_log REOPEN + adjudication REJECT + `HAVING SUM >= threshold`；gaps `cps_inspection_item CROSS JOIN cps_room LEFT JOIN cps_room_check_record` + gapDays 参数化默认 7

**Python 侧（basic-project `1cbd9cc`，pytest 604p/2f[既有]/1s，+36 覆盖套件）**
- `app/skill/coverage/{domain,infrastructure,application,api}/`：CoverageSnapshot/Summary dataclass + CoverageClient（4 端点 + iter_all_pages + 指数退避）+ CoverageAnalysisService（4 分析 + summary + kinds 子集 + 异常指标）+ CoverageIngestionService
- 路由：`GET /api/v1/coverage/analyze` + `POST /api/v1/coverage/snapshot`（cps_admin）
- 复用 `memory_metric_snapshot` 表（不新建 schema）
- ruff 全绿；36 新测试覆盖 9 repository + 12 application + 5 java_client + 10 router
- 文档：`docs/波次10-Python侧-FR-10覆盖分析视图与服务.md`

**FR-10 完成度**：四分析（频率/区域主管负载/复发/覆盖缺口）全部上线，含异常指标聚合（top_3 高频/top_3 超期主管负载/top_3 复发/按 storageRoomType 聚合缺口）。memory_metric_snapshot 表已写记录，给 FR-12 效果评估留口。

## 波次 11（2026-09-27，FR-11 procedural 调度记忆 + FR-12 效果评估）

**Java 侧（cps `98c56c8`，mvn 281/0，+4 契约测试）**
- `/api/cps/admin/memory/dispatcher`：INNER JOIN cps_issue + GROUP BY 4 维（category_l1_id+area+decision+ai_relation）+ GROUP_CONCAT+SPLIT+SUBSTRING_INDEX 取最近 5 条裁决 reason 截断 200 字
- `/api/cps/admin/coverage/effect`：4 metric（aiPassRate/humanOverrideRate/avgCloseDurationHours/recurrenceRate），前 3 INNER JOIN cps_issue，recurrenceRate UNION（flow_log.REOPEN + adjudication.REJECT），NULLIF 防 0 兜底
- 真实库 smoke：dispatcher 0（无裁决）/ effect 4 行 metric

**Python 侧（basic-project `2c5811e`，pytest 634p/2f[既有]/1s，+30 新增）**
- `app/skill/procedural_memory/{domain,infrastructure,application,api}/`：`DispatchPattern`/`DispatchHint` dataclass + `ProceduralMemoryService`（三段加权评分 0.33×3 + consolidate 合并幂等）+ 3 端点 `/procedural/{retrieve,record,consolidate}`
- `app/skill/effect_evaluation/`：`EffectMetric`/`EffectComparison` + `EffectEvaluationService`（4 metric + 周期 delta_pct + 阈值回归检测 5%/10%/20%）+ 3 端点 `/effect/{snapshot,compare,regression}`
- 新 ORM 表 `dispatch_pattern`（PG/SQLite 双方言复用 MemoryRepository 模式）+ `memory_metric_snapshot` 复用
- Identity.require('cps_admin') 守护；新路由注册到 `app/api/v1/router.py`
- 文档：`docs/波次11-Python侧-FR-11调度记忆+FR-12效果评估.md`

**I 线全部完成**：
| FR | 状态 | 波次 |
|---|---|---|
| FR-09 长期记忆 | ✅ | 波次 9 |
| FR-10 覆盖分析 | ✅ | 波次 10 |
| FR-11 procedural 调度记忆 | ✅ | 波次 11 |
| FR-12 效果评估 | ✅ | 波次 11 |

## 波次 12（2026-09-27，B6 视觉点检 Agent，F 线核心部件）

**Java 侧（cps `fd5e9f6`，mvn 302/0，+21）**
- V20261006：`cps_vision_check_record`（16 列 + 4 索引 + 4 CHECK 约束）+ `cps_vision_check_judge_event`（7 事件类型）
- **关键决策**：用新表名 + `Vision` 前缀避 B3 同名表冲突；human_override 不覆写 `aiOverall`（受 CHECK 约束）
- `/api/cps/room-checks` 4 端点（submit/list/override/rejudge）+ `CpsVisionClient` 60s 超时
- 真实库 smoke 全过：submit 201 / list 200 / override on TIMEOUT 409 / rejudge on TIMEOUT 200
- 事件流串联：SUBMITTED → AI_STARTED → AI_FAILED → TIMEOUT_OPENED → REJUDGED → HUMAN_OVERRIDE

**Python 侧（basic-project `c3911e0`，pytest 686p/2f[既有]/1s，+52）**
- `app/projects/room_checks/{domain,infrastructure,application,api}/` 三级判定
- 评分公式：`score = 100 - (content_mismatch*40) - (keywords_missing*20) - (evidence_vague*30) - (other*10)`
- 边界：≥80 PASS / 50-79 PARTIAL / <50 PROBLEM
- 失败 fallback：PROBLEM/0 + 「视觉判定超时/失败，建议人工复检」
- 进程内 LRU+TTL 幂等（1800s / 1024）
- Java callback 客户端：POST `/api/cps/room-checks/{fingerprint}/callback` + 30s timeout + 2 retries + backoff (1s, 3s) + fingerprint URL 注入防御
- 端点：`POST /api/v1/agent/room-checks/v2/judge`（cps_admin）/ `v2/rejudge`（cps_admin + cps_supervisor 双角色）

**B6 联调 TODO（已写入文档）**：真实 qwen-vl-plus + 真实图片 / callback 持久化重试队列（Redis Streams/DB） / 多实例 cache 共享（Redis） / 真实 RustFS 拉图 / Java 端契约联调

## 波次 13（2026-09-27，C7 系统化测试 + J 线联调验收）

### 双仓完成
| 仓 | commit | 测试 | 关键产物 |
|---|---|---|---|
| **cps** | `993abac` | mvn **311/0**（基线 302 + C7 新增 9） | `CpsWave13IntegrationTest`（4 链 23 断言 + 5 不变量）；MockRestServiceServer 替身走 J 联调；文档 `cps/docs/波次13-Java侧-C7系统化测试与J联调.md`（122 行） |
| **basic-project** | `42f7ad8` | pytest **707/2f/1s**（基线 686 + C7 新增 21） | `tests/c7/test_c7_e2e_chains.py`（5 链 17 测试 + J 4 不变量）；FakeMemoryRetriever/Coverage/Callback 替身；文档 `docs/波次13-Python侧-C7系统化测试与J联调.md`（130 行） |

### Java 4 链
- **Chain 1** AI 初审+裁决+记忆闭环：agent/rectifications → handleCallback → memory/entries → memory/retrieve，7 断言 + 1 幂等子测试
- **Chain 2** B6 视觉点检：submit→PASS→override→HUMAN_OVERRIDE，5 断言（验 ai_overall 不被覆写）
- **Chain 3** B7 覆盖分析五视图：frequency/regionSupervisor/recurrence/coverageGaps/effect + dispatcherMemory + 4 metric，7 断言
- **Chain 4** F 线 D-22 推送：seed INTERFACE + PUT 加 EMAIL + enable=true 但 endpoint 空 400 + 未知 channel 400，4 断言

### Python 5 链
- **Chain 1** AI 记忆闭环：submit → retrieve 拼 prompt → 二次检索 1→2 变化
- **Chain 2** B7 覆盖分析：analyze 端到端 + 4 anomalies key + 403 鉴权
- **Chain 3** B6 视觉点检：三级 PASS + 满分 + 3 模型调用
- **Chain 4** 结构化输出 v2：extract_json_object 4 级降级（直解→栅栏→平衡→重试）
- **Chain 5** 回调重推：re-push 200 + 幂等键不变 + running 409

### 关键不变量（5 Java + 4 Python）
- `cps_issue` ↔ `cps_memory_entry` 双向参照一致
- `cps_initial_review_event` 12 类全集（反射 recordEvent 4 参）
- `cps_review_adjudication` `uk(issue_id, version_no)` 幂等
- `cps_vision_check_judge_event` 7 类全集
- `cps_push_config` UNIQUE + CHECK(id=1) + CHECK(channel 白名单)
- Python：initial_review 5 状态 + replayed 观察项 / memory_entry UNIQUE + 软取代 / weekly_report_run 5 状态 + push_status 独立 / inspection_plan 三类任务 LRU+TTL 幂等

### 取舍与新发现
1. **PG pgvector 扩展未装** → docker-compose 加 `pgvector/pgvector:pg16` image（**F 线真用之前必做**）
2. **Python `audit_snapshot.memory_hints_used` ORM 字段缺失** → 链1 走协议层兜底；真落库需 ORM 加列 + alembic 迁移
3. **/coverage/{rooms,lines,processes,inspections,categories} v2 五视图端点 Python 端未实现** → 走 /coverage/analyze 替代（**B7 v2 端点正式化是后续波次任务**）

### C7/J 线总览
- **C7 系统化测试**：✅ 9（Java）+ 21（Python）= 30 个新测试
- **J 联调验收**：✅ 双仓真实库（Java 用本地 MySQL + MockRestServiceServer；Python 用 PG+SQLite 双栈 + Fake 替身）；J 联调 TODO（B6 联调遗留）已部分覆盖（Java 端契约联调完成 + Python 端 callback 持久化/多实例 cache 仍遗留）
