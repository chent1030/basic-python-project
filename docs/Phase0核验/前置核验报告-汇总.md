# CPS 开发 Phase 0 前置核验报告（汇总）—— M0 决策输入

> 编制：主会话（汇总软件架构师 + 后端架构师双侧报告）
> 分报告：【前置核验报告-Java侧.md】／【前置核验报告-Python侧.md】（同目录）
> 对应计划：CPS智能巡检系统开发计划.md §4.2（T0.1~T0.6）

## 1. 总体结论

| 核验项 | 完成度 | 说明 |
|---|---|---|
| T0.1 旧单兼容盘点 | ✅ 代码级完成 | 状态机/接口/表全景 + flow_version 落点建议（Java 侧报告 §②） |
| T0.2 通道核查 | ✅ 代码级完成 | Java↔Python 唯一接通链路 = 创建问题触发巡检（C-10）；C-01~C-09 双侧全绿地 |
| T0.3 附件通道 | ✅ 代码级完成 / ⚠️ 发现断点 bug | 新附件已走 RustFS object_key，但转发链路断（见 R-N1） |
| T0.4 mock 排查 | ✅ 完成 | Java 主代码零 mock（形态是"缺失"非"假实现"）；Python 无假实现，skills/ 空 |
| T0.5 环境与基线 | ✅ 静态完成 | 8080/8000/9000/8090/8008/5432 存活；MySQL 启动方式待验证（R-N3） |
| T0.6 报告+决策会 | 本文件 | M0 决策输入见 §4 |

**Go/No-Go：A1（Java）与 A5（Python）可立即启动**；计划主体成立、无需重排；线 I 维持 26.5 人天上限假设但确认全部为绿地工作量（见 R-N4）。

## 2. 关键发现合并

| # | 发现 | 证据 | 影响 |
|---|---|---|---|
| F-1 | Java→Python 契约 C-01/C-02/C-03/C-07/C-09 全缺失，唯一客户端仅 createAndStart + runtimeStatus | `cps/backend/src/main/java/com/company/cps/service/CpsAgentFrameworkClient.java`；9 个 Controller 无 `/api/callbacks/**` | 即 A1 工作量本身，按计划推进 |
| F-2 | flow_version 最小侵入落点：`cps_issue` 加列（空表零迁移风险）+ `CpsIssueService.executeAction` 路由 + 旧 `CpsWorkflowStateMachine` 冻结、新建 V2 | Java 侧报告 §②，`CpsIssueService.java:133-165` | D-01 决策输入就绪 |
| F-3 | **附件断点（P0 bug）**：新附件 content 恒 NULL（已走 RustFS），但客户端 `content==null` 静默跳过 → evidence 不再传 Python，线上实为"无图巡检" | `CpsAgentFrameworkClient.java:55-56` | R-02 具体化，必须波次 1 修复 |
| F-4 | MySQL 真相：`application.yml:8` 本机直连 `jdbc:mysql://localhost:3306/cps`，TCP 可达、实例可用；但 docker 无 MySQL 容器，重启可恢复性未知 | Java 侧报告 §③ | 新风险 R-N3 |
| F-5 | 七对象无一落 PG：全部为 SQLite JSON 文档（`app/harness/kernel/infrastructure/sqlite.py:13-104`）；alembic 仅 1 head（只建 items 表）；AgentRun 缺 model_version/confidence/duration_ms；Report 无 weekly_run_id | Python 侧报告 §② | 线 I 绿地确认（R-N4） |
| F-6 | Python 附件 = base64 全量内嵌 SQLite（`service.py:161-172`，上限 10MB）；无 S3/RustFS 客户端代码与依赖 | Python 侧报告 §④ | object_key 演进 100% 绿地，波次 2+ 排期 |
| F-7 | vector-service(8008) 已部署但应用零调用（相似图检索为进程内 cosine） | Python 侧报告 §④ | 架构遗留决策（挂决策清单） |
| F-8 | **`config/agents.yaml:8` 明文提交真实 dashscope API key** | Python 侧报告 | 安全问题 R-N2，建议当日轮换 |
| F-9 | D-22 口径矛盾：后端设计 §3.4:186 说 L 按 code point，§3.4:188 示例"两个 U+2026 → L 贡献 6"（UTF-8 字节读法） | Python 侧报告 §⑥ | A5 单测前必须裁决（见 §4） |
| F-10 | Java 侧技术债三件：`CpsIssueService.java:404` empName 工号占位、Controller `DEV_EMP` 无鉴权兜底、`cps_reminder_rule` 死表、seed 脚本 `V999999` 已损坏 | Java 侧报告 §④ | 记入债务清单，seed 修复挂 Phase 0 收尾 |

## 3. 新增风险与计划修正

| # | 风险 | 级别 | 处置 |
|---|---|---|---|
| R-N1 | 附件断点 → 无图巡检 | **P0** | 插入波次 1：Java 客户端按 fileUrl 从 RustFS 读流转发（保留 base64 兼容） |
| R-N2 | 明文 API key 已入 git | 高 | 代码改 env 优先（波次 1）；密钥轮换由用户执行 |
| R-N3 | MySQL 启动方式/可恢复性未证实 | 中 | 待运行验证（下次环境重启时确认；不阻塞开发） |
| R-N4 | 线 I（七对象 PG 化）全绿地 | 中 | 维持 26.5 人天上限；优先级后移不占关键路径；波次 2 起随 C 线一并建 PG 表 |
| R-N5 | vector-service 用或弃 | 低 | 挂决策清单 D-14 附注，波次 2 评审 |

## 4. M0 决策输入（A 类）

| # | 事项 | 建议/裁决 | 状态 |
|---|---|---|---|
| D-01 | 旧单切换判定 | `cps_issue.flow_version VARCHAR(16) NOT NULL DEFAULT 'legacy'`（空表现状零迁移风险）；`executeAction` 按 flow_version 路由；旧状态机冻结、新建 V2（含 PENDING_AI_REVIEW/PENDING_REVIEWER_CONFIG） | 暂定-待用户终审 |
| D-22 | 文本计量口径 | **建议按字符数（code point）**：与 PRD §29.2 `len(S)` 语义一致（Python len() 即 code point）；`……` 在 L 中计 2、在 P 中计 1；后端设计文档 §3.4:188 示例的"6"系 UTF-8 字节读法，判为笔误。其余 4 个口径按 Python 侧报告 §⑥ 冻结表执行 | 暂定-待用户终审（本次提问确认） |
| D-02 | 周报分类清单 | 不阻塞波次 1；维持"先 CPS 问题巡检 + 辅房点检两类、配置驱动"暂定 | 待 M0 决策会 |
| D-04 | 事项必选/厂区映射/提醒渠道 | 不阻塞波次 1 | 待 M0 决策会 |

## 5. 波次 1 开发范围（本日启动）

| 侧 | 任务 | 对应计划 |
|---|---|---|
| Java（后端架构师） | 附件断点修复（R-N1）→ Flyway 迁移（cps_issue 扩展列 + 4 张新表）→ V2 状态机 + flow_version 路由 → C-01 客户端 + C-02 回调端点 + C-03 查询 + 30s 超时扫描骨架 → mvn test 证据 | 线 A1 + R-N1 修复 |
| Python（软件架构师） | D-22 口径常量模块 → check_text_rules 纯函数 + 穷举边界单测 → alembic 建 agent_initial_review_exec（PG）→ C-01/C-02/C-03 端点与回调客户端（视觉/语义检查显式标注"待 A6/A7/A8"，不伪造通过）→ API key env 优先 → pytest 证据 | 线 A5 + 契约骨架 |

## 6. 遗留待运行验证清单

1. MySQL 容器化/重启恢复（R-N3）；2. 附件 RustFS 读流转发的真实大图联测（波次 1 后 J1 前补）；3. T0.3 异常注入（弱网/重试）待联调环境；4. V999999 seed 脚本修复（Phase 0 收尾）。
