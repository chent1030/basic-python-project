# Docker 可访问 Docker Hub 时保持默认值。
PYTHON_BASE_IMAGE=python:3.11-slim
PIP_INDEX_URL=https://pypi.org/simple

# 如果服务器使用公司镜像仓库，改为完整地址，例如：
# PYTHON_BASE_IMAGE=registry.company.local/dockerhub/library/python:3.11-slim
# 模型父目录：该目录下必须存在 siglip2-so400m-patch14-384
SIGLIP_MODEL_DIR=/Users/csai/project/models
